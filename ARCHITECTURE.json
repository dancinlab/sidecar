{
  "schemaVersion": "2.0",
  "kind": "architecture-tree",
  "title": "sidecar — 아키텍처 SSOT",
  "summary": "AI 코딩 사이드카 엔진. 핵심: 규칙은 코드가 아니라 데이터 — 엔진(언제·어떻게 검사)은 공유, 규칙(무엇을 검사)만 per-repo 주입.",
  "viewer": "ARCHITECTURE.html",
  "serve": "python3 serve.py  (로컬 서버 + 브라우저 자동 오픈; file:// fetch 차단 우회)",
  "convergence": {
    "note": "재발방지 학습 SSOT (구 인라인 마커 이관 · root-cause). state=ossified|in_flight|failed · value=핵심 · threshold=재발조건/해결 · source=원파일. 같은 결함 재발 시 여기 항목을 update-in-place 로 추가/갱신(코드 인라인 주석 금지).",
    "records": [
      {
        "id": "EXEC_SPAWN_ERROR_UNHANDLED",
        "state": "ossified",
        "value": "execArgs must handle the child's `error` event (spawn ENOENT / EACCES) — degrade to a non-zero ExecResult, never let an unhandled `error` crash the process",
        "threshold": "a minimal-PATH hook env (SessionStart) couldn't spawn `bash` → unhandled 'error' event killed the whole SessionStart hook (node:events crash); every execShell caller was exposed",
        "source": "lib/exec.ts"
      },
      {
        "id": "CLAUDEMD_NOT_L0",
        "state": "ossified",
        "value": "CLAUDE.md must never be classified L0 — it DECLARES the L0 list (fromMarkdown) and is re-injected each turn, it is not protected engine core",
        "threshold": "the L0 path-parser regex must NOT include a CLAUDE*.md alternation, or the guide captures itself",
        "source": "lib/lockdown.ts"
      },
      {
        "id": "ARCH_SNAPSHOT_NOT_HISTORY",
        "state": "ossified",
        "value": "ARCHITECTURE.json/.md is a CURRENT-STATE snapshot tree, not a change log — 'update' means replace the affected node in-place and delete the old wording; NEVER append history/version/dated/previous/deprecated nodes (history → CHANGELOG + git). The inject note carries this every turn so the model stops accreting history into the tree",
        "threshold": "the word '갱신형'(updatable) was read as 'add an update entry', so sessions kept leaving version/dated/'이전엔…' nodes in the tree; hardened the every-turn inject note + commons c4 to say snapshot/replace-in-place explicitly",
        "source": "modules/architecture.ts"
      },
      {
        "id": "CI_TRACK_NATIVE",
        "state": "ossified",
        "value": "PR CI status is tracked by `sidecar ci-track` (gh pr checks --json → aggregate + verdict + optional --watch/--merge-on-green), not by hand-rolled `gh pr checks|grep` + /tmp monitor sleep loops",
        "threshold": "merge-on-green campaigns repeatedly re-implemented CI polling inline (e.g. /tmp/pr3688_mon.sh) — no sidecar command tracked remote CI; this centralizes it (the poll runs inside the CLI, not a bash sleep loop)",
        "source": "modules/ci-track.ts"
      },
      {
        "id": "NO_RAW_CLOUD_CLI",
        "state": "ossified",
        "value": "raw runpodctl/vastai/`cloud rent`/provider-API calls are blocked in code (pre bash), not just by an enforcement regex rule",
        "threshold": "a session ran `runpodctl pod create`/`cloud rent` directly because c11 was only a hint+warn; code guard runs before config rules with no override",
        "source": "modules/cloud-guard.ts"
      },
      {
        "id": "NO_RAW_DOJO_DECK",
        "state": "ossified",
        "value": "raw `python train.py`/`torchrun`/`accelerate launch`/`deepspeed` training launches AND hand-run run.sh in a dojo/decks tree are blocked in code — use `hexa dojo`/`hexa deck`",
        "threshold": "c11 named dojo/deck builtins but only cloud CLI was code-guarded; training launches and deck run.sh slipped through",
        "source": "modules/cloud-guard.ts"
      },
      {
        "id": "NO_HANDROLLED_SHARD_FANOUT",
        "state": "in_flight",
        "value": "a hand-rolled launcher loop (split -n l/N + a for/while loop that setsid/nohup-backgrounds repeated `hexa run`/training launches) is the exact bypass that defeated NO_RAW_CLOUD_CLI — copy-to is whitelisted so the .sh sails through, then the fanout runs remotely unseen. Detected as a WARN that redirects to `hexa cloud fire-shards`",
        "threshold": "a session wrote /tmp/h1305_launch.sh (12-shard staggered `hexa run` decode loop) + `hexa cloud copy-to` + remote run, bypassing structured dispatch/register/cost-accounting; the guard only saw the whitelisted copy-to",
        "source": "modules/cloud-guard.ts"
      },
      {
        "id": "QUOTE_AWARE_SEGMENT",
        "state": "ossified",
        "value": "command-head detection segments on UNQUOTED shell operators only — a `|`/`;`/`&` inside quotes is data, not a separator",
        "threshold": "`grep -E \"vast|runpod\"` false-blocked once `vast`/`runpod` became unconditional CLI heads: stripQuotes ran before the `|`-split, so the quoted alternation was torn into a bare `vast` segment",
        "source": "modules/cloud-guard.ts"
      },
      {
        "id": "NO_VAST_VERB_WHITELIST",
        "state": "ossified",
        "value": "`vast` is blocked unconditionally in command position, same as `vastai`/`runpodctl` — NO verb whitelist",
        "threshold": "a verb whitelist (VAST_VERBS) only blocked ~10 verbs; `vast set api-key`/`vast scp`/`vast execute`/literally `vast cli` all leaked because their verb wasn't listed — the guard 're-unlocked' every time vast.ai added a subcommand",
        "source": "modules/cloud-guard.ts"
      },
      {
        "id": "BOTH_RUNPOD_CLIS",
        "state": "ossified",
        "value": "both RunPod CLI heads (`runpodctl` Go + `runpod` Python) are blocked, plus the serverless host `api.runpod.ai` alongside the control-plane `api.runpod.io`/`rest.runpod.io`",
        "threshold": "only `runpodctl` was listed, so `runpod config`/`runpod project deploy`/`runpod pod create` and serverless `curl api.runpod.ai/v2/<id>/run` all leaked — same whitelist-too-narrow bypass as VAST_VERBS",
        "source": "modules/cloud-guard.ts"
      },
      {
        "id": "DOJO_TRAIN_NAME_BROAD",
        "state": "ossified",
        "value": "the `(train|finetune|sft|pretrain)*.py` script match is INTENTIONALLY broad — it over-blocks helpers like `train_utils.py`/`trainer.py` too. Name alone can't separate a launcher (train_lora.py) from a helper (train_utils.py), and this guard is no-override, so we bias to false-positive: a missed launch (FN) leaks uncounted GPU $, a false block (FP) is recoverable (route via `hexa dojo`, or rename the non-launch script)",
        "threshold": "someone proposes narrowing the regex (e.g. only `train.py`) to cut FPs — DON'T: it reopens the FN hole (run_training.py, train_model.py launchers slip through). Keep broad; accept the FP.",
        "source": "modules/cloud-guard.ts"
      },
      {
        "id": "COMPANION_SURFACE_NOT_INJECTED",
        "state": "ossified",
        "value": "a sibling CLI (hexa) and its subcommands (cloud/atlas/verify/drill) existed and worked, but the sidecar injected only its OWN command catalog (toolkit) — never the neighbour CLI's surface — so the agent re-probed 'is hexa installed? does cloud exist?' every session",
        "threshold": "toolkit injected sidecar commands only; no carrier surfaced an adjacent project-CLI's verb list, so each session started blind to hexa",
        "source": "modules/companions.ts"
      },
      {
        "id": "CODE_GUARD_DESTRUCTIVE_BYPASS",
        "state": "ossified",
        "value": "--no-verify (gate bypass), git reset --hard/clean -fd (working-tree destroy), rm -rf / ~ $HOME (catastrophic), curl|wget|sh (remote code exec) are blocked in CODE before config rules — not regex-only — so a profile edit can't disable them; inline `# ...-ok` markers still allow an explicit per-command override",
        "threshold": "these were block-policy but enforcement.json-only; a regex/profile edit (or the stdin-input bug that disabled the whole pre layer) would have left the irreversible/gate-bypass commands unguarded",
        "source": "modules/danger-guard.ts"
      },
      {
        "id": "STALE_BRANCH_TRAP",
        "state": "in_flight",
        "value": "SessionStart surfaces HEAD vs origin/<default> (behind/ahead/detached); BEHIND or detached → loud ⚠️ stale-branch warning so the agent re-checks origin before trusting code (and doesn't duplicate already-merged work)",
        "threshold": "a session started on a stale feature branch behind origin/main, read pre-merge code as current, and rebuilt an already-merged fix (#3736 dup of #3734); no guard flagged HEAD≠merged-tip",
        "source": "modules/git-context.ts"
      },
      {
        "id": "FORCE_LEASE_DUAL_SSOT",
        "state": "ossified",
        "value": "`--force-with-lease` is ALLOWED by both the code guard (git-guard.ts) and config enforcement.json (H-FORCE-PUSH exceptions) — only blind --force/-f/+refspec is blocked, with a `# force-ok` escape",
        "threshold": "QA found git-guard blocked --force-with-lease while config exempted it → lease hard-blocked with no override, breaking the rebase→lease-push workflow; aligned code guard to config intent",
        "source": "modules/git-guard.ts"
      },
      {
        "id": "ING_STALENESS_NUDGE",
        "state": "in_flight",
        "value": "code edited ≫ ing untouched is WARNED at Stop (c6) — code edits bump a counter, ing add/next/done resets it, Stop warns once past editThreshold then resets; warn-only (forcing an ing update has no ground truth, so a hard block would be a false-positive factory)",
        "threshold": "every-turn `ing inject` only SHOWED the board; sessions edited lots of code while the board went stale because nothing nudged an update",
        "source": "modules/ing-staleness.ts"
      },
      {
        "id": "ING_BOARD_DEDICATED_REF",
        "state": "ossified",
        "value": "board lives on a dedicated `ing` ref via plumbing, never the working tree",
        "threshold": "tracked worktree file: branch-switch/reset clobbers session edits (happened); untracked: unshared",
        "source": "modules/ing.ts"
      },
      {
        "id": "ING_NO_DIRECT_MAIN_PUSH",
        "state": "failed",
        "value": "never store/push the board on main",
        "threshold": "protectedBranches repo blocks direct main push so the board is unusable; a dedicated ref is protected-main-safe",
        "source": "modules/ing.ts"
      },
      {
        "id": "INIT-INJECT-DUP",
        "state": "ossified",
        "value": "init --hooks scaffolded the full host-wide inject set (commons/recommend/prefs/easy/load/architecture) into per-repo .claude/settings.json, duplicating the global install (~/.claude/settings.json) and/or the enabled plugin — same context injected 2-3x/turn, burying short user prompts",
        "threshold": "resolved: per-repo .claude/settings.json is BANNED — sidecar is global-only. init no longer scaffolds repo hooks (the --hooks flag is gone); hooks live solely in the global ~/.claude/settings.json via `sidecar install`. inject = host-wide policy, the global layer owns it",
        "source": "modules/init.ts"
      },
      {
        "id": "HELP_NO_RAW_BACKTICK",
        "state": "ossified",
        "value": "a bare backtick in the cli HELP template literal terminates it → help/build break; lint HELP-BACKTICK(block) scans the HELP block and flags any unescaped backtick (escaped \\\\\\` allowed)",
        "threshold": "twice in one session a help-line edit added `code`-style backticks (fleet-full, folders), each silently breaking `sidecar help` until caught by hand — ossify as a commit gate",
        "source": "modules/lint.ts"
      },
      {
        "id": "MAC_OOM_FANOUT_JETSAM",
        "state": "in_flight",
        "value": "a 16GB Mac OOM-dies (jetsam kills apps + kernel panic) because parallel fan-out (cycle-all/all-bg-go) accumulates 6+ detached `claude` processes (~400-490MB each) across sessions until macOS jetsams — mem-guard adds a free-RAM preflight before background-spawn + an opt-in launchd notify watchdog",
        "threshold": "JetsamEvent reports 6/13-6/18 + panic 6/17 showed six `2.1.179` (claude) procs as top memory holders at OOM time; preflight warns/blocks spawn when available RAM < warnPct",
        "source": "modules/mem-guard.ts"
      },
      {
        "id": "NAMING_VERSION_SUFFIX",
        "state": "ossified",
        "value": "new files/dirs named foo_v2/_final/_copy/_old (via Write/Edit OR a mv/cp/touch/mkdir bash command) are BLOCKED — history belongs in git, not the filename; the `@canonical-ok` (write) / `# canonical-ok` (bash) marker is the only override",
        "threshold": "warn-only proved too weak — the stale-sibling pileup kept recurring, so the user escalated it to a hard block + added bash-command coverage (a CLI `mv a a_v2.ts` previously slipped past the Write-only guard)",
        "source": "modules/naming-guard.ts"
      },
      {
        "id": "PRETOOLUSE_INPUT_FROM_STDIN",
        "state": "ossified",
        "value": "PreToolUse tool input must be read from STDIN (CC pipes the hook payload there), not only from $CLAUDE_TOOL_INPUT env — that env var is unset by current Claude Code, so reading env-only made every code guard (raw `vastai`/`runpodctl` block, force-push) a silent pass",
        "threshold": "a session ran raw `vastai`/`runpodctl` despite the ossified NO_RAW_CLOUD_CLI code guard because parseToolInput read an env var CC never set; verified by piping the real {tool_input:{command}} payload on stdin",
        "source": "modules/pre.ts"
      },
      {
        "id": "PRETOOLUSE_DENY_SCHEMA",
        "state": "ossified",
        "value": "pre-hook block must emit hookSpecificOutput.permissionDecision=deny (current PreToolUse schema), not the legacy {decision:block} which current Claude Code ignores — else the guard prints but never blocks",
        "threshold": "a session ran raw `vastai destroy` despite c11 because emitBlock emitted only {decision:block}+exit0, which CC silently dropped; verified the new schema blocks via the actual `pre bash` hook output",
        "source": "modules/pre.ts"
      },
      {
        "id": "ARXIV_RATELIMIT_NOT_NORESULTS",
        "state": "ossified",
        "value": "arXiv throttles a burst with EITHER a 'Rate exceeded.' text body OR an HTTP 503 page — both have 0 <entry> exactly like a genuine empty result. Detect throttle by status>=500 OR /rate exceeded/ OR a non-Atom body (no <feed/<entry); a real empty result IS a valid <feed> with totalResults 0. Auto-retry with backoff (notice each round so the agent recognizes a RATE problem), then loudFail with a rate-limit error; NEVER report 'no results' for a throttle",
        "threshold": "checked only the 'rate exceeded' string, so an HTTP-503 throttle slipped through and the command said 'no results' for a query with thousands of papers",
        "source": "modules/research.ts"
      },
      {
        "id": "CODE_GUARD_SECRET_LITERAL",
        "state": "ossified",
        "value": "hardcoded API keys / private keys / provider tokens are blocked in CODE at write time (before config rules) — a committed secret is an irreversible git-history leak; regex-only enforcement could be disabled by a profile edit",
        "threshold": "H-SECRET-LITERAL was block-policy but enforcement.json-only; the stdin-input bug (and any profile edit) would have left credential writes unguarded",
        "source": "modules/secret-guard.ts"
      },
      {
        "id": "SHADOW_PREMARKER_STALE",
        "state": "ossified",
        "value": "pre-marker shadows in ~/.claude/commands/ lack SHADOW_MARKER, so refresh skips them as 'hand-authored' and they go stale — 18/22 lost the source description's `Triggers —` line, so bare-text recognition of /fleet /sbs /easy … silently broke even though source commands/*.md were correct",
        "threshold": "resolved: `sidecar shadow --force` overwrites marker-less collisions from source (source = SSOT); run it once per host to heal pre-marker shadows. Re-breaks only if a NEW marker-less shadow layer is introduced without a heal path",
        "source": "modules/shadow.ts"
      },
      {
        "id": "SHADOW_GEN_NATIVE",
        "state": "ossified",
        "value": "bare /cmd shadows are generated by `sidecar shadow` from sidecar's own commands/ — not by the retired sidecar package, and never baked to a $CLAUDE_PLUGIN_ROOT/sidecar-cache path",
        "threshold": "a session's /arxiv etc. died because sidecar-generated shadows pointed at a removed ~/.claude/plugins/cache/sidecar path; sidecar now owns shadow generation, marker-tracked + regenerable",
        "source": "modules/shadow.ts"
      },
      {
        "id": "SHADOW_MARKER_AFTER_FRONTMATTER",
        "state": "ossified",
        "value": "the SHADOW_MARKER must be inserted AFTER the YAML frontmatter block, never prepended — Claude Code only parses `description:` when `---` is on line 1; a marker on line 1 pushed frontmatter down so the picker rendered the marker comment as the command's description",
        "threshold": "if the marker is ever prepended again, every shadow command shows '<!-- sidecar-shadow ... -->' instead of its description in the slash picker",
        "source": "modules/shadow.ts"
      },
      {
        "id": "SHIP_PROPAGATE_ALL_SURFACES",
        "state": "ossified",
        "value": "merge+self-update+shadow must run as one unit",
        "threshold": "adding a slash command then running only pr-cycle+self-update left the shadow mirror stale → /fleet-abstract invisible; ship bundles all three",
        "source": "modules/ship.ts"
      },
      {
        "id": "NO_SCATTER_STATE_DIR",
        "state": "ossified",
        "value": "work output (verdicts·bench·experiments·scratch) goes in the single git-tracked `state/` root — creating `.verdicts/`·`bench/`·`experiments/`·`scripts/scratch/` (via Write OR a mkdir/touch/cp/mv bash command) is BLOCKED; `# state-ok`(bash)/`@state-ok`(content) overrides",
        "threshold": "preserve-state was governance-only with no code teeth — sessions kept standing up scatter dirs that rot untracked; escalated to a hard guard mirroring naming-guard after the user asked to enforce the位置(location) family",
        "source": "modules/state-guard.ts"
      },
      {
        "id": "TOOLKIT_FROM_HELP_SSOT",
        "state": "ossified",
        "value": "the agent-facing command catalog (TOOLKIT.jsonl) is GENERATED from the HELP text in cli/index.ts, never hand-maintained in parallel; `toolkit check` snapshot-diffs committed vs generated so the two can't drift",
        "threshold": "commands existed + worked but had no proactive catalog/triggers, so the agent didn't know to use them (arxiv/secret blind spot); a hand-kept second catalog would just drift from help",
        "source": "modules/toolkit.ts"
      },
      {
        "id": "HELP_CLOSE_DELIM_NEWLINE",
        "state": "ossified",
        "value": "the closing delimiter search must be the line-start `\\\\n\\\\`;`, NOT a bare `\\\\`;` — the HELP body contains escaped backticks (e.g. `worktree gc\\\\`;`) and a bare-`\\\\`;` search truncated there, silently dropping every command after worktree (atlas/convergence/ing/sync/upstream/verdict)",
        "threshold": "bare backtick-semicolon search matched an escaped inline backtick mid-body and cut the catalog short",
        "source": "modules/toolkit.ts"
      },
      {
        "id": "WORKTREE_GONE_ONLY_PILEUP",
        "state": "ossified",
        "value": "gc/pr-cycle reaped ONLY [gone]-upstream worktrees; squash-merge + no-push AGENT worktrees never get [gone], so they were skipped forever and piled (67 wts / 84GB / 6 days). Fix: reap on [gone] OR HEAD-age > worktree.maxAgeDays, preserving the tip under refs/reaped/ first so aged work stays recoverable",
        "threshold": "agent worktree clean+unlocked+not-recent and older than maxAgeDays, or [gone] upstream",
        "source": "modules/worktree.ts"
      },
      {
        "id": "ARCH_INJECT_IGNORED",
        "state": "in_flight",
        "value": "갱신 지시가 트리 앞 괄호에 묻혀 모델이 시켜야만 갱신 — 턴마감 게이트를 트리 뒤(recency)로 이동 + 갱신 시 보고 의무화",
        "threshold": "ARCHITECTURE inject 가 갱신을 능동적으로 안 하고 사용자 지시에만 반응",
        "source": "modules/architecture.ts"
      }
    ]
  },
  "columns": [
    {
      "key": "이름",
      "label": "이름",
      "tree": true
    },
    {
      "key": "역할",
      "label": "역할"
    },
    {
      "key": "구분",
      "label": "구분"
    },
    {
      "key": "상세",
      "label": "상세"
    }
  ],
  "tree": {
    "이름": "sidecar",
    "역할": "프로젝트-무관 AI 코딩 사이드카 — 작업 흐름에 끼어드는 게이트 집합",
    "구분": "root",
    "상세": "엔진(.ts)은 모든 repo 공유, 규칙(JSON)만 주입. 1–4층은 5층(규칙 데이터)을 하드코딩하지 않는다.",
    "children": [
      {
        "이름": "surfaces — 5가지 통합 지점",
        "역할": "에이전트가 무언가 하기 직전/직후 = 사이드카가 끼어들 자리",
        "구분": "group",
        "상세": "어떤 에이전트 런타임이든 이 5 hook으로 환원된다.",
        "children": [
          {
            "이름": "UserPromptSubmit",
            "역할": "사용자가 말함 → prompt",
            "구분": "surface",
            "상세": "키워드→플레이북 매핑, 위험어→힌트. 결과: stderr(컨텍스트 주입)"
          },
          {
            "이름": "PreToolUse(Bash)",
            "역할": "도구 호출 직전 → pre bash",
            "구분": "surface",
            "상세": "코드레벨 가드(아래 children) 를 먼저 평가 → 그 뒤 config enforcement 정규식 매칭(backup). 코드 가드는 config 보다 먼저 실행되고 끌 수 없다(profile 편집 무력화 방지) · 인라인 `# ...-ok` 마커만 예외. block=stdout JSON / warn=stderr",
            "children": [
              {
                "이름": "force-push 가드",
                "역할": "히스토리 재작성 push 차단",
                "구분": "guard",
                "상세": "blind --force/-f/+refspec 차단 · --force-with-lease 허용 · `# force-ok` 이스케이프 — config H-FORCE-PUSH 와 동일 정책"
              },
              {
                "이름": "cloud-raw 가드",
                "역할": "raw 클라우드 CLI 직접 호출 차단",
                "구분": "guard",
                "상세": "c11 — 구조화 dispatch 를 우회하는 raw runpodctl/vastai/ssh/scp/curl 직접 호출을 가드(`hexa cloud`/pod 흐름으로 유도)"
              },
              {
                "이름": "danger 가드",
                "역할": "파괴적 명령 차단",
                "구분": "guard",
                "상세": "no-verify·reset-hard·curl|sh 는 상시 always-on · rm-rf-root(루트 자체 /, /*, ~, $HOME, *)는 config `dangerGuard.rmRfRoot` 토글 · 기본 false=OFF(opt-out)"
              },
              {
                "이름": "hand-rolled-fanout 가드",
                "역할": "손수 fanout 루프 경고",
                "구분": "guard",
                "상세": "warn — copy-to 안의 손수 런처 fanout 은 구조화 dispatch·비용계상을 우회 → `fire-shards` 로 유도"
              },
              {
                "이름": "mem-guard",
                "역할": "OOM 예방",
                "구분": "guard",
                "상세": "background-spawn(`&`/nohup/disown) 직전 free-RAM 체크 → warnPct 이하 warn·blockPct 이하 block · OOM jetsam 예방"
              },
              {
                "이름": "naming-guard",
                "역할": "비-canonical 작명 차단(생성) + 터치 시 넛지",
                "구분": "guard",
                "상세": "버전/복사 접미사 파일·폴더명(`_v2`·`_copy`·`foo 2`…). 새 생성 BLOCK → canonical update-in-place 강제(Write/Edit + Bash mv/cp/touch/mkdir). 기존 비표준 파일 터치는 warn-only 넛지(생성만 막던 갭 — 편집 허용해 고치게). c25 · `@canonical-ok`/`# canonical-ok` 면제 · test/spec 제외"
              },
              {
                "이름": "state-guard",
                "역할": "산출물 흩뿌림 차단",
                "구분": "guard",
                "상세": "scatter 디렉토리(`.verdicts`·`bench`·`experiments`·`scripts/scratch`…) 생성 BLOCK → 산출물은 단일 git-tracked `state/` 루트 강제(commons preserve-state)·Write/Edit + Bash mkdir/touch/cp/mv 양쪽·`state/`·`build/`·`.harness/` 하위 통과·`@state-ok`/`# state-ok` 면제"
              },
              {
                "이름": "single-doc(scatter) 가드",
                "역할": "흩어진 문서 차단",
                "구분": "guard",
                "상세": "scatter 파일명(`*-report/summary/notes/plan/guide.md`·`UPPERCASE.md`·날짜접두 `.md`) 생성 BLOCK(scope 무관·`DOC-SCATTER`) → ARCHITECTURE(SSOT)+CHANGELOG(append) 통합 · quickref 규율(`DOC-NO-QUICKREF`)은 scopeDirs 한정 warn · `docs.allow` 리스트·`docs.enforce:off` 로 면제 (commons single-doc)"
              }
            ]
          },
          {
            "이름": "PreToolUse(Write)",
            "역할": "쓰기 직전 → pre write",
            "구분": "surface",
            "상세": "경로·내용·우회패턴 매칭 + write-time hard-deny(상세는 각 4g/4h 노드): SKILL-DESC-CAP/CMD-DESC-LONG(명령 desc 1400 천장+320 미니멀) · COMMONS-PROSE/NO-DODONT/DODONT-INCOMPLETE · DODONT-LONG · SECRET-LITERAL · HANDOFF-SCATTER. stdout JSON(deny)/stderr(warn)"
          },
          {
            "이름": "PostToolUse",
            "역할": "도구 호출 직후 → post bash / edit",
            "구분": "surface",
            "상세": "exit/파일경로로 실패 라우팅, L0 편집 경고, c6 ing-staleness(코드 편집 카운트 bump). errors 큐 / stderr"
          },
          {
            "이름": "Stop",
            "역할": "에이전트 응답 종료 → 마지막 assistant 메시지 기반 3 Stop 훅",
            "구분": "surface",
            "상세": "transcript 의 마지막 assistant 텍스트를 읽어 후처리하는 3 훅 (아래). 공통 리더 lastAssistantText(modules/recommend.ts · export).",
            "children": [
              {
                "이름": "recommend stop-check",
                "역할": "박스로 종료 시 auto-proceed 강제 (정밀 decision:block)",
                "구분": "guard",
                "상세": "non-present default mode 인데 답변이 **진짜 박스/auto-pick 줄로 끝나면**(endsOnBox=마지막 ~2 비어있지않은 줄 검사) `decision:block` 으로 모델 재호출=강제. 정밀: tail=2 라 박스 뒤 작업/요약 따라오면 무발화 → 정상 땐 'Stop hook error' 안 뜨고 진짜 박스-멈춤만 강제(에러≠노이즈). 루프가드=stop_hook_active. (modules/recommend.ts)"
              },
              {
                "이름": "convergence stop-check",
                "역할": "에이전트 출력의 재발-신호 → SSOT 기록 넛지",
                "구분": "guard",
                "상세": "warn-only(stderr·non-block) — `ing staleness-check` 동형(decision:block 아님). 에이전트 자신의 마지막 메시지를 재발-신호 키워드(config/convergence-triggers.json)로 스캔 → 매치 시 SSOT 기록 넛지를 stderr 표면화. 키워드=넓은 그물·정밀도는 에이전트 판단. once-가드=신호별 1회 → 오탐이 진짜 넛지 소진 안 함"
              },
              {
                "이름": "ing staleness-check",
                "역할": "코드 다수 편집인데 ing 보드 무변동 경고 (c6)",
                "구분": "guard",
                "상세": "warn-only(stderr) — 코드 ≥editThreshold(기본 5) 편집했는데 ing 보드 무변동이면 1회 경고 후 카운터 리셋. 강제 불가(ground truth 없음). (modules/ing.ts)"
              }
            ]
          },
          {
            "이름": "PreCompact·PostCompact",
            "역할": "자동 컴팩션 전/후 → 세션-스코프 inject 재주입(컴팩션 생존)",
            "구분": "surface",
            "상세": "매-턴 inject(commons·recommend·prefs·easy)는 UserPromptSubmit 라 컴팩션 후 복귀하지만, 세션-스코프 inject(architecture·git-context·toolkit·companions·ing)는 SessionStart 에서만 떠 컴팩션 시 증발 → PreCompact(요약기가 보도록)+PostCompact(새 윈도우 직후)에 그 6개 재주입. additionalContext(`hookEventName` echo)"
          },
          {
            "이름": "체크포인트",
            "역할": "commit/세션 → lint / verify / ing",
            "구분": "surface",
            "상세": "검증·신선도·인계. exit code + stderr"
          }
        ]
      },
      {
        "이름": "layers — 층 구조",
        "역할": "1=바닥(실행 입구) → 5=꼭대기(규칙 데이터)",
        "구분": "group",
        "상세": "재사용성의 비밀: 1–4층이 5층을 하드코딩하지 않는다.",
        "children": [
          {
            "이름": "L1 실행 입구",
            "역할": "bin/sidecar — 런타임 자동탐색",
            "구분": "layer · 공유 엔진",
            "상세": ""
          },
          {
            "이름": "L2 디스패처",
            "역할": "cli/index.ts — 명령 라우팅 진입점",
            "구분": "layer · 공유 엔진",
            "상세": ""
          },
          {
            "이름": "L3 공용 부품",
            "역할": "lib/ — 경로찾기·로깅·실행·직렬화",
            "구분": "layer · 공유 엔진",
            "상세": "",
            "children": [
              {
                "이름": "paths.ts",
                "역할": "repo-root 상향 탐색",
                "구분": "lib",
                "상세": "config.json→.git 순, SIDECAR_REPO_ROOT override"
              },
              {
                "이름": "config.ts",
                "역할": "harness.config.json 읽어 기본값과 머지",
                "구분": "lib",
                "상세": "모든 모듈의 단일 설정원"
              },
              {
                "이름": "log.ts",
                "역할": "구조화 로그",
                "구분": "lib",
                "상세": "info/ok/warn/loudFail"
              },
              {
                "이름": "json.ts",
                "역할": "JSON 직렬화 유틸",
                "구분": "lib",
                "상세": ""
              },
              {
                "이름": "exec.ts",
                "역할": "셸/프로세스 실행",
                "구분": "lib",
                "상세": "execShell=bash -lc, execArgs=직접 spawn"
              },
              {
                "이름": "lockdown.ts",
                "역할": "L0 잠금 파일 검사 (opt-in)",
                "구분": "lib",
                "상세": ""
              }
            ]
          },
          {
            "이름": "L4 기능 모듈",
            "역할": "modules/*.ts — 언제·어떻게 검사할까",
            "구분": "layer · 공유 엔진",
            "상세": "① 완전 범용 등급만 포함",
            "children": [
              {
                "이름": "pre",
                "역할": "정규식 규칙 엔진",
                "구분": "module",
                "상세": "규칙은 전부 외부 JSON"
              },
              {
                "이름": "post",
                "역할": "exit/파일경로만 봄",
                "구분": "module",
                "상세": "도메인 지식 0"
              },
              {
                "이름": "prompt",
                "역할": "키워드→힌트 매핑",
                "구분": "module",
                "상세": "키워드는 외부 JSON"
              },
              {
                "이름": "lint",
                "역할": "staged-L0·신선도·수렴 + doc-gate (commit 전 게이트)",
                "구분": "module",
                "상세": "staged 변경에 대한 commit-time 게이트 모음. CHANGELOG·ARCHITECTURE(.json|.md)·README 미동반=block (pr-cycle은 ing 보드 현행화도 gate). 개별 체크는 children.",
                "children": [
                  {
                    "이름": "SHADOW-DESC (4f)",
                    "역할": "명령 desc 인지 (warn)",
                    "구분": "check",
                    "상세": "commands/*.md description 이 ≤1400자 skill-listing cap + `Triggers —` 절 보유인지 — 누락 시 bare /cmd 자연어 인지가 죽으므로 환기(sidecar-repo 한정·commands/ 존재 시). sidecar s18 포팅"
                  },
                  {
                    "이름": "CMD-DESC-LONG (4f-min)",
                    "역할": "명령 desc 미니멀 cap (block)",
                    "구분": "check",
                    "상세": "commands/*.md description codepoint ≤ `lint.cmdDescCap`(기본 320)인지 — 1400 천장(SHADOW-DESC)과 별개의 빡빡한 '미니멀' 선. 초과면 차단: 하는 일+`Triggers —` 만 두고 플래그표/서브버브 카탈로그는 body/`--help`/`argument-hint` 로. write-time 도 같은 cap deny(`shadow.ts:descWriteViolation`→pre.ts) · 0=off"
                  },
                  {
                    "이름": "INJECT-OVERSIZED (4j)",
                    "역할": "inject별 개별 바이트 cap (block)",
                    "구분": "check",
                    "상세": "각 inject 소스가 **자기 cap** 이하인지 — `lint.injectCaps` 맵(path→budget · `/` 끝=그 dir `*.md` 각각 · 기본 recommend.md 7000·styles/ 9000·prefs.json 2000). 산문 비대=매 턴 context 세금이라 소스를 lean 하게(emit 절단=손실 금지). commons/CLAUDE/ARCHITECTURE 는 각자 format lint 가 그들의 inject lint. 0=off"
                  },
                  {
                    "이름": "COMMONS-PROSE/NO-DODONT/DODONT-INCOMPLETE (4g)",
                    "역할": "commons·CLAUDE do/dont 양식 (block)",
                    "구분": "check",
                    "상세": "**commons.md(또는 .harness override) + 루트 CLAUDE.md** 각 `## slug` 섹션이 `- do:` AND `- dont:` 줄만(둘 다 필수·산문 금지)인지 — 첫 `## ` 앞 preamble 면제 · 서브폴더 CLAUDE.md(folder-docs)는 자유양식 제외. 거버넌스/프로젝트규칙 SSOT 산문 재비대 + 한쪽-누락 차단(`commons.ts:lintCommonsFormat`+lintCommonsText)"
                  },
                  {
                    "이름": "DODONT-LONG (4h)",
                    "역할": "do/dont 길이 cap (block)",
                    "구분": "check",
                    "상세": "staged **commons.md + 루트 CLAUDE.md** 의 do/dont **엔트리**(=`- do:` 줄 + 뒤따르는 `  ` 연속줄 전부 합산 → 연속줄로 쪼개 cap 우회 차단) codepoint 길이가 `lint.dodontCap`(기본 200) 초과 신규/증가면 차단. HEAD diff-aware 라 기존 긴 줄 grandfather · Write·Edit 커버 · 서브폴더 CLAUDE.md 제외. archive_sidecar tape-lint #2 포팅"
                  },
                  {
                    "이름": "FOLDER-GUIDE-MISSING (4i)",
                    "역할": "폴더 가이드 (block)",
                    "구분": "check",
                    "상세": "staged 폴더(folderGuides)에 CLAUDE.md 없으면 차단(존재만·folder-docs)"
                  },
                  {
                    "이름": "HELP-BACKTICK (4j)",
                    "역할": "HELP 백틱 위생 (block)",
                    "구분": "check",
                    "상세": "cli/index.ts HELP 리터럴 내 이스케이프 안 된 백틱 차단(help/build 깨짐 재발 박제·텍스트스캔)"
                  }
                ]
              },
              {
                "이름": "naming",
                "역할": "repo 전수 비-canonical 이름 audit (write-guard 의 backlog 짝)",
                "구분": "module",
                "상세": "`audit [path] [--ing] [--gate]` — git ls-files 트리에서 버전/복사/중복 접미사 이름을 전수 스캔(`offendingToken` 재사용 = naming-guard 와 동일 판정). write-time naming-guard 는 새 파일만 막아 기존 backlog 를 못 보던 갭을 메움.",
                "children": [
                  {
                    "이름": "출력 모드",
                    "구분": "detail",
                    "상세": "기본 read-only 보고(exit 0) · `--gate`=위반 시 exit 1(commit/CI 게이트) · `--ing`=요약 1줄을 **그 repo 자기 ING** 보드에 add(보드=내 repo 전용 · cross-repo 전달 없음). cross-repo 감사는 각 repo 안에서 돌려 그 repo ING 에 착지."
                  },
                  {
                    "이름": "생태계-native 면제 (오탐 방지)",
                    "구분": "detail",
                    "상세": "native-canonical-first: Android res 한정자 `-v<API>`(`values-v26`·`mipmap-anydpi-v26`)·bare `_version` 모듈명(`version\\d+` 만 위반)은 guard/audit 공통 면제 · audit 은 `archive/`·`.verdicts/` 트리 무시(frozen 이력·전이 로그)."
                  }
                ]
              },
              {
                "이름": "install",
                "역할": "공용(전역) 부트스트랩 — clone+래퍼+전역훅 (init=per-repo 와 별개)",
                "구분": "module",
                "상세": "SSOT=scripts/install.sh(curl 가능); `sidecar install`은 이를 위임. clone dancinlab/sidecar→~/.sidecar/cli(있으면 ff) · 래퍼(심볼릭 아님)→~/.local/bin/sidecar · install-hooks --global. 멱등 · 플래그 --no-hooks/--ref/--dir/--bin/--dry-run · 갱신은 self-update"
              },
              {
                "이름": "shadow",
                "역할": "sidecar commands/ → ~/.claude/commands/ 미러 (bare /cmd 생성)",
                "구분": "module",
                "상세": "plan|remove|--force. 각 commands/*.md 를 ~/.claude/commands/ 에 얇은 /cmd 위임자로 복제 + SHADOW_MARKER 주석으로 추적 — 손수작성 동명파일은 skip. 사이드카 무의존·재생성 가능(플러그인 미설치 폴백).",
                "children": [
                  {
                    "이름": "--force heal",
                    "역할": "마커-없는 stale 치유",
                    "구분": "detail",
                    "상세": "`--force` 는 마커-없는 충돌도 source(SSOT)로 덮어써 마커 도입 *전* stale shadow 를 heal (@convergence SHADOW_PREMARKER_STALE: 마커없는 옛 shadow 가 손수작성 false-positive 로 skip 돼 source 의 `Triggers —` 줄을 잃으면 맨-텍스트 명령 인지가 죽는다)."
                  },
                  {
                    "이름": "마커 위치",
                    "역할": "frontmatter 뒤 삽입",
                    "구분": "detail",
                    "상세": "마커는 frontmatter 닫는 `---` **뒤**에 삽입(`withMarker`) — Claude Code 는 `---` 가 1행일 때만 `description:` 을 읽으므로 앞에 붙이면 picker 가 마커를 desc 로 표시(@convergence SHADOW_MARKER_AFTER_FRONTMATTER). desc 품질은 lint 4f(SHADOW-DESC) 게이트."
                  },
                  {
                    "이름": "hexa 위임자",
                    "역할": "이웃 CLI 위임",
                    "구분": "detail",
                    "상세": "일부 commands/*.md 는 `hexa` 위임자(/hexa·/cloud·/dojo·/deck·/verify·/atlas — hexa 부재 시 graceful echo; sidecar 자체 dojo 는 /hdojo). ⚠️ frontmatter argument-hint 중첩 큰따옴표 금지(YAML 깨짐) — 안쪽은 작은따옴표."
                  }
                ]
              },
              {
                "이름": "pr-cycle",
                "역할": "push→PR→검증머지→로컬 base sync",
                "구분": "module",
                "상세": "feature 브랜치에서 doc-gate→push→gh pr create→self-merge(squash/merge/rebase fallback·CI pending 재시도)→머지 커밋이 origin/base 에 올랐는지 검증→로컬 base ff-sync→merged worktree sweep→stale-PR reaper. main/master 에선 거부.",
                "children": [
                  {
                    "이름": "로컬 ff-sync",
                    "역할": "로컬 base 뒤처짐 방지",
                    "구분": "detail",
                    "상세": "`git fetch origin <base>:<base>` · checkout 전환 없이 로컬 main 뒤처짐 방지 · non-ff 거부."
                  },
                  {
                    "이름": "stale-PR reaper",
                    "역할": "내 열린 PR 전수 정리",
                    "구분": "detail",
                    "상세": "내 다른 열린 PR 전수: MERGEABLE=자동 squash머지 · CONFLICTING/blocked=rebase/`gh pr close` 안내와 함께 큰소리 보고 · `--no-reap` 차단. 중단/실패한 머지가 영구 방치(충돌로 썩음)되던 누수 차단."
                  },
                  {
                    "이름": "worktree 누적 한계",
                    "역할": "[gone] 못 거두는 워크트리",
                    "구분": "detail",
                    "상세": "sweep 은 자기 머지 브랜치만 [gone] 으로 걷음 — fleet/sub-agent 의 isolation:worktree 워크트리는 개별 push/삭제가 없어 [gone] 이 안 떠 누적. SessionStart `worktree gc` 의 age 백스톱(HEAD>maxAgeDays 기본3일 → 팁을 refs/reaped/ 보존 후 reap)이 수거."
                  }
                ]
              },
              {
                "이름": "ship",
                "역할": "구현 후 모든 설치 surface 에 한 번에 전파 (orchestrator)",
                "구분": "module",
                "상세": "pr-cycle(검증 머지) → self-update(전역 CLI git-pull) → shadow(commands/ 재미러) 를 한 명령으로 올바른 순서 실행, 단계 실패 시 즉시 중단. shadow 빠뜨리면 새 슬래시가 picker 에 안 떠 '반영 안됨' 재발 → 셋을 묶어 락스텝. 플러그인(/plugin) 설치본은 별도 surface · `--no-doc` pr-cycle 로 forward"
              },
              {
                "이름": "verify",
                "역할": "검증 명령 병렬 실행",
                "구분": "module",
                "상세": "명령은 config 선언"
              },
              {
                "이름": "ci",
                "역할": "검증 명령 병렬 실행 + CI scaffold",
                "구분": "module",
                "상세": "`ci [all|fast|list]` = config `verify.checks` 병렬(실패 1개라도 exit 1). `ci scaffold` = `.github/workflows/ci.yml` 방출. `ci.fallback` = cost-free: `pick-runner` 가 self-hosted pool probe → 무료 self-hosted·없으면 github-hosted fallback. `ci.cachePaths` → `actions/cache@v4` warm"
              },
              {
                "이름": "paper",
                "역할": "demiurge 하우스 논문 스캐폴드·표지·빌드·배포(Zenodo/arXiv)",
                "구분": "module",
                "상세": "new(스캐폴드) → cover(imagine 표지) → build(xelatex+bibtex×3) → publish/update/unpublish/status(배포). NeuroLM 정답지 미러 + 하드 content-floor 게이트.",
                "children": [
                  {
                    "이름": "build content-floor 게이트",
                    "구분": "detail",
                    "상세": "build=xelatex+bibtex×3 · pages/refs/result-figs 보고 · 하드 2게이트 ≥10p(g51) + ≥9 result figs(cover 제외 = NeuroLM바) · 둘다 `--min-*` 0 으로 해제 · 미달 exit3 · 깨진/빈 PDF 실패처리."
                  },
                  {
                    "이름": "NeuroLM 정답지 미러",
                    "구분": "detail",
                    "상세": "외부 품질 정답지 NeuroLM(templates/paper/_reference_samples/2409.00101_neurolm.pdf · ICLR 2025 · 읽기전용) 미러 — 섹션 Intro·Background·Method·Experiments·Ablation·Discussion + 결과그림(TikZ 아키텍처 + pgfplots 막대 · 캡션=결과연결) · g5 tier-badge · natbib. 마케팅 표지는 허용(의도된 divergence). 손조립 규율을 도구로 박제."
                  },
                  {
                    "이름": "배포 수명주기 (Zenodo/arXiv)",
                    "구분": "detail",
                    "상세": "publish/update/unpublish/status `--to zenodo|arxiv|both` · 키는 secret · 원장 PAPERS/<slug>/publish.json. Zenodo=완전 REST(업로드→메타→발행→DOI · update=new-version · unpublish=DRAFT 삭제). arXiv=제출 API 없음(SWORD 폐지)→tar.gz 패키징 + 제출 가이드(가짜 자동제출 안 함)"
                  }
                ]
              },
              {
                "이름": "imagine",
                "역할": "AI 이미지+영상 생성 (fal/openai · 키 secret 경유)",
                "구분": "module",
                "상세": "출력 확장자로 분기: 이미지(.png/.jpg)=fal `openai/gpt-image-2`(또는 -b openai); 영상(.mp4/.mov)=fal Seedance 2.0(`-i` 없으면 text-to-video·있으면 image-to-video). 동일 queue+poll · 프롬프트는 FILE · 키는 secret get·-K curl 로 argv 누출 방지 · -m override"
              },
              {
                "이름": "email (alias mail)",
                "역할": "Postmark 트랜잭션 메일 발송 (토큰 secret 경유)",
                "구분": "module",
                "상세": "`POST /email`(Postmark REST). 서버 토큰=`secret get postmark.server_token`·`-K` curl 로 argv 누출 0. send: --to/--from/--subject + 본문(--text/--html=FILE·-m 인라인) + cc/bcc/reply-to/tag/stream + --attach + --dry. history=outbound API·list=토큰 점검. 성공 조용·실패 큰소리 + email.jsonl 원장"
              },
              {
                "이름": "kick (alias drill)",
                "역할": "hexa-lang gap-breakthrough/discovery 엔진 패스스루",
                "구분": "module",
                "상세": "`hexa kick --seed \"<seed>\"` 래퍼. bare 인자→--seed 합치기 · 선두 플래그(--rounds N·--engine mk9|mk10)→verbatim 패스스루. 긴 실행 → stdio inherit(라이브 스트림·timeout 없음). hexa PATH 없으면 127+안내"
              },
              {
                "이름": "fleet (+ lab/abstract/full)",
                "역할": "영구 멀티레인 오케스트레이터 런북 (fire-on-arrival · 4모드)",
                "구분": "module",
                "상세": "MODES 맵(fleet·lab·abstract·full)이 엔진 1개를 공유 — roster 파일·런북·어휘만 다르다. 4모드 공통 go/stop/status, `/fleet <mode> …`는 sidecar 가 `$ARGUMENTS` forward.",
                "children": [
                  {
                    "이름": "fleet (기본)",
                    "역할": "빌드/멀티레인",
                    "구분": "mode",
                    "상세": "roster `.harness/fleet/active` + templates/fleet.md. N개 독립 레인을 background Agent로, 각 레인은 착륙 즉시 다음 라운드를 스스로 발사(barrier 없음)."
                  },
                  {
                    "이름": "fleet lab",
                    "역할": "research 특화",
                    "구분": "mode",
                    "상세": "roster `lab` + fleet-lab.md. 레인=막힌 프론티어(벽). 싼 research gate→정당화 시만 비싼 implement/measure→측정(c2)→SSOT(blocking-frontiers)→재-research/벽(🧱 reopenable)."
                  },
                  {
                    "이름": "fleet abstract",
                    "역할": "추상화 특화",
                    "구분": "mode",
                    "상세": "roster `abstract` + fleet-abstract.md. 레인=누적 법칙군/천장. census LAWS→한 겹 peel해 공유 trade-off(메타법칙) 도출→escape 원리 발명→반증가능 예측으로 캐스팅해 compute/research 핸드오프. 메타법칙(🌌)은 새 lens로 reopen, 산출 전부 미검증(d6 좌표≠발견), lazy-ceiling 금지(c14 d)."
                  },
                  {
                    "이름": "fleet full",
                    "역할": "풀스택",
                    "구분": "mode",
                    "상세": "roster `full` + fleet-full.md. 프론티어가 3 페이즈 순환 research→implement→(벽)→abstract→falsify — implement 스킵 금지(약한 레버도 싼 probe 로 벽 측정 후 abstract). 기본 순차(afg)·`parallel` 일 때만 Workflow fan-out. 게이트는 비용 — 싼/로컬 자동·비싼(pool/GPU)만 4축+go. depletion=양축 dry"
                  }
                ]
              },
              {
                "이름": "errors",
                "역할": "(kind,code)→severity 분류",
                "구분": "module",
                "상세": "맵은 외부 JSON"
              },
              {
                "이름": "ledger",
                "역할": "에이전트 작업 등록 (중복 방지)",
                "구분": "module",
                "상세": "도메인 무관"
              },
              {
                "이름": "ing",
                "역할": "진행보드 (전용 `ing` git ref · 내 repo 전용)",
                "구분": "module",
                "상세": "work/next/pod · 보드는 내 현재 repo 전용 — cross-repo 전달(`--to`)은 폐기(직접수정 원칙 · commons upstream-fix). upstream 결함은 그 repo 에서 직접 고쳐 머지하고, 끊기는 작업만 `↩resume` 으로 내 보드에 남긴다.",
                "children": [
                  {
                    "이름": "저장 = ing ref (worktree 아님)",
                    "구분": "detail",
                    "상세": "done=scrub. 보드는 refs/heads/ing 에 plumbing(hash-object→mktree→commit-tree→update-ref) 저장 + best-effort push origin ing → branch-switch/reset 무관·공유·protected-main-safe. 보드 cwd=`git rev-parse --show-toplevel`(REPO_ROOT fallback)로 해석 — config-less worktree 가 sibling repo ref 에 쓰던 누수 차단"
                  },
                  {
                    "이름": "verb 의미 · stdin",
                    "구분": "detail",
                    "상세": "add=work · next=다음 · done은 id 정확매칭 우선, text fallback은 1건일 때만(대량삭제 방지). `↩`-prefix resume 항목은 show/inject 맨 앞 표면화(c17 — upstream-fix 중단작업 복원). add/next 자유텍스트에 셸 특수문자(괄호·따옴표·$·→)면 `--stdin`(또는 단독 `-`)로 stdin 읽기 — 슬래시 `$ARGUMENTS` 무인용 깨짐 회피."
                  },
                  {
                    "이름": "적체 방지 (나이 게이트)",
                    "구분": "detail",
                    "상세": "inject/show 가 각 work 항목에 나이 `⏳Nd` 표시 + `ing.staleDays`(기본 5) 초과 묵음 또는 `ing.maxActive`(기본 12) 초과 시 매 턴 `🧹 ING 적체 — … 끝난 항목 지금 done 로 scrub` 강제 지시. 자동삭제 아님 — 완료분이 ACTIVE 로 썩는 것 차단(나이는 done-vs-active proxy)."
                  }
                ]
              },
              {
                "이름": "bitter-gate",
                "역할": "JSONL 빈도 분석",
                "구분": "module",
                "상세": "dormant 규칙 폐기 검토"
              },
              {
                "이름": "audit",
                "역할": "JSONL 카운트 기반 6축 스코어카드",
                "구분": "module",
                "상세": ""
              },
              {
                "이름": "gc",
                "역할": "마크다운 가이드 링크 drift 탐지",
                "구분": "module",
                "상세": ""
              },
              {
                "이름": "architecture",
                "역할": "설계 SSOT 주입 (SessionStart + 매 UserPromptSubmit)",
                "구분": "module",
                "상세": "ARCHITECTURE.json(우선)/.md 를 additionalContext 로 주입 — SessionStart 1회 + 매 UserPromptSubmit 재주입으로 설계 트리를 commons급 상주. 매-턴 재주입은 `convergence` 배열 제외(매 턴 ~21KB 절감)·80KB 절단·부재 무음. 트리 뒤에 턴-마감 게이트(`🏛️ ARCHITECTURE 갱신: …`) 부착",
                "children": [
                  {
                    "이름": "lint (트리 위생 · c4 강제)",
                    "역할": "fine-decomposition 게이트",
                    "구분": "detail",
                    "상세": "`architecture lint` = 셀 길이 cap(ARCH-BIG-CELL · `lint.archCellCap` 기본 300), piled ` · ` 항목>`archPiledMax`(6)(ARCH-PILED), history 키(previous/deprecated/… = ARCH-HISTORY) 검사. severity-map ARCH-*=block 라 `sidecar lint` 가 위반 차단 → 트리가 산문 leaf 로 비대 안 됨. 위반은 커널만 남기거나 children 으로 분해. 최상위 `convergence` 제외"
                  },
                  {
                    "이름": "convergence 재발학습 store (구 convergence 모듈 흡수)",
                    "역할": "재발방지 학습 SSOT — 인라인 @convergence 마커 폐기",
                    "구분": "detail",
                    "상세": "재발방지 학습은 인라인 마커가 아니라 ARCHITECTURE.json 최상위 `convergence.records[]`(id·state·value·threshold·source) 한곳에 산다(single-doc). CRUD=`architecture convergence {list|add|rm|edit}`(id-keyed·upsert). `lintConvergenceRecords`=커밋 게이트(CONVERGENCE-MALFORMED). `convergenceForFile`(터치 시 주입)은 현재 비활성(pre.ts 주석)"
                  }
                ]
              },
              {
                "이름": "git-context",
                "역할": "stale-branch 함정 가드 (SessionStart inject)",
                "구분": "module",
                "상세": "HEAD vs origin/<default>(main|master) 를 LOCAL ref 로 비교(fetch 없음) → BEHIND 이거나 detached-stale 이면 ⚠️ 경고 주입: '이 위치 파일은 최신이 아닐 수 있다 · git log origin/<default> -- <file> 로 이미 머지됐는지 확인 후 코드 믿어라'. clean 한 default 브랜치면 무음(노이즈 0). 재발방지: 옛 feature 브랜치에서 옛 코드를 보고 이미 머지된 작업을 중복 구현한 사고(#3736 dup #3734) 차단"
              },
              {
                "이름": "claudemd",
                "역할": "프로젝트 규칙 매턴 재주입",
                "구분": "module",
                "상세": "repo-root CLAUDE.md 를 매 UserPromptSubmit 마다 additionalContext 로 재주입(SessionStart 1회로는 긴 대화에서 묻힘) → 프로젝트 규칙을 commons급 salience 로 상주(MUST-FOLLOW 헤더 prepend). `<!-- enforce:start/end -->` 있으면 그 섹션만·없으면 전체·80KB 절단·부재 무음. 디렉토리·모듈 트리는 ARCHITECTURE.json SSOT 라 CLAUDE.md 에 중복 안 함"
              },
              {
                "이름": "load",
                "역할": "매턴 macOS 자원 압박 readout 주입 (CPU+RAM+swap, ⚠️ on danger)",
                "구분": "module",
                "상세": "show|inject. 매 UserPromptSubmit 에 한 줄 readout 을 additionalContext 로 주입 — 답변 맨 위에 부하를 상주시켜 무거운 작업 전 자각시킴. 비-macOS 무음.",
                "children": [
                  {
                    "이름": "3축 신호등",
                    "역할": "CPU·RAM·swap·wt readout",
                    "구분": "detail",
                    "상세": "CPU(load1÷cores·🟢<0.7 🟡<1.0 🔴≥1.0), RAM((active+wired+compressor)%+pressure), swap(🟢<2G 🟡<6G 🔴≥6G), wt(추가 worktree·🟢0-2 🟡3-9 🔴≥10). 어느 축이든 🔴/pressure≥warn 이면 선두 `⚠️` 마커 + 가드 노트(대량빌드·병렬agent·GPU 자제)"
                  },
                  {
                    "이름": "동기·비용안전",
                    "역할": "왜 MEMORY 를 보나",
                    "구분": "detail",
                    "상세": "부하로 죽는 맥은 CPU 가 아니라 MEMORY(compressor+swap 폭증)로 죽으므로 RAM압박·swap 을 같이 본다. 비용안전: sysctl+vm_stat 만(즉답) — 전체 스캔으로 수초 걸리는 `memory_pressure` CLI 는 매턴 훅에 부적합이라 의도적 회피."
                  }
                ]
              },
              {
                "이름": "toolkit",
                "역할": "명령 카탈로그 — agent 가 전 명령을 인지·사용하도록 SessionStart 주입 (sidecar TOOLKIT 패리티)",
                "구분": "module",
                "상세": "list|inject|json|write|check. 단일 출처=cli/index.ts `HELP` 텍스트 파싱 + keyword 트리거 매핑. inject=SessionStart 에 전 명령을 compact 카탈로그(`id — use ⟨triggers⟩`)로 주입(키워드 사각지대 보완). write=TOOLKIT.jsonl 산출. check=dispatch case 커버리지 + 커밋본 drift(`lint` TOOLKIT-DRIFT warn)"
              },
              {
                "이름": "companions",
                "역할": "이웃 CLI 명령 surface 주입 — agent 가 sibling 툴체인(hexa 등)을 더듬지 않고 인지하게 SessionStart 주입",
                "구분": "module",
                "상세": "toolkit 의 자매 — toolkit 이 sidecar 자신 명령을 주입하면 companions 는 이웃 CLI(hexa 등) 카탈로그를 주입. DOMAIN-AGNOSTIC: 어떤 CLI 는 데이터(harness.config.json `companions` + host `~/.sidecar/companions.json` union)라 엔진에 hexa 안 박음. 각 companion 카탈로그(default `--help`) 실행·절단·주입·부재/실패 skip"
              },
              {
                "이름": "sync",
                "역할": "공유파일 sync 셸 스크립트 래퍼",
                "구분": "module",
                "상세": ""
              },
              {
                "이름": "pool",
                "역할": "호스트 로스터 + 원격 실행 + 자원 프로브",
                "구분": "module",
                "상세": "shared:false 제한 호스트는 allow 컨텍스트 밖에서 on/status 차단(allow 마커가 cwd 일치 시 해제·status 🔓 표기). ssh=execArgs 직접 spawn(로컬 $ 전개 방지). specs 가 코어/메모리/GPU 를 POSIX-sh 프로브해 캐시 · list 는 라이브 부하(CPU%·GPU util)를 SSH 병렬 프로브 ⚡뱃지(캐시 안 함)"
              }
            ]
          },
          {
            "이름": "L5 규칙 데이터",
            "역할": "per-repo — 무엇을 막을까",
            "구분": "layer · per-repo",
            "상세": "이 층만 프로젝트별로 달라짐",
            "children": [
              {
                "이름": "enforcement.json",
                "역할": "pre 정규식 규칙",
                "구분": "data",
                "상세": "strict 기본(15룰 block-everything) — init 이 항상 이 세트를 깐다(과거 hardcore profile 흡수, 별도 변종 없음)."
              },
              {
                "이름": "keywords.json",
                "역할": "prompt 트리거 키워드",
                "구분": "data",
                "상세": ""
              },
              {
                "이름": "severity-map.json",
                "역할": "errors 분류 맵",
                "구분": "data",
                "상세": "fallback=block · 낮은 unknown_threshold (strict 기본)."
              },
              {
                "이름": "harness.config.json",
                "역할": "repo 루트 설정",
                "구분": "data",
                "상세": "project·lockdown·verify.checks 등"
              }
            ]
          }
        ]
      },
      {
        "이름": "plugin — 배포 (자기완결 · 공용셋)",
        "역할": "sidecar 를 Claude Code 플러그인으로 배포 — 훅 + 슬래시 명령 + CLI 본체가 ONE 유닛. 프로젝트 무관·중앙 1곳",
        "구분": "group",
        "상세": "SELF-CONTAINED: marketplace.json(source=`.`=repo 루트가 플러그인) → plugin.json(`commands: []`=명령 네임스페이스 OFF, 중복 방지). 훅은 `${CLAUDE_PLUGIN_ROOT}/bin/sidecar` 실행 → `/plugin update`+reload 로 CLI+hooks 동시 갱신. 슬래시는 `shadow` 가 bare /cmd 미러·전역 `sidecar` 는 폴백",
        "children": [
          {
            "이름": "hooks/hooks.json + hooks/run.sh",
            "역할": "거버넌스 훅 배선 (guards + injects + SessionStart) → run.sh 디스패처",
            "구분": "config",
            "상세": "run.sh = 단일 해석점: ${CLAUDE_PLUGIN_ROOT}/bin/sidecar(번들) 우선 → 전역 `sidecar` 폴백 → 둘 다 없으면 exit 0(무음). CC가 stdin 으로 주는 페이로드를 exec 로 상속(pre 가 stdin 에서 tool input 읽음)"
          },
          {
            "이름": "commands/*.md",
            "역할": "슬래시-명령 위임자 셋 (bare /cmd · shadow 가 미러하는 SOURCE)",
            "구분": "config",
            "상세": "프런트매터(description+Triggers+argument-hint+allowed-tools) + `!sidecar <cmd> $ARGUMENTS` 위임자. 플러그인 명령으로 로드 안 됨(plugin.json `commands: []` → bare /cmd 와 `/sidecar:cmd` picker 중복 방지). `shadow` 가 ~/.claude/commands/ 에 bare /cmd 미러. 공용셋·프로젝트 무관 · hook-내부 제외"
          }
        ]
      },
      {
        "이름": "module-grading — 모듈 분류",
        "역할": "'규칙을 JSON으로 빼면 다른 repo에서 그대로 도나?'",
        "구분": "group",
        "상세": "YES면 ①, NO면 ③",
        "children": [
          {
            "이름": "① 완전 범용",
            "역할": "이 repo에 포함",
            "구분": "grade",
            "상세": "pre/post/prompt/lint/verify/errors/ledger/bitter-gate/audit/gc/ing/convergence/sync/pool/architecture/claudemd"
          },
          {
            "이름": "② 부분 범용",
            "역할": "패턴만 문서화, 코드 미포함",
            "구분": "grade",
            "상세": "구현이 특정 디렉토리 규약에 묶임 (extending.md)"
          },
          {
            "이름": "③ 도메인 전용",
            "역할": "포함 금지",
            "구분": "grade",
            "상세": "deploy/db/ssh/health/site/정산 등 특정 인프라·비즈니스에 묶임"
          }
        ]
      },
      {
        "이름": "config-driven — config 주도 설계",
        "역할": "lib/config.ts가 harness.config.json을 읽어 머지",
        "구분": "group",
        "상세": "설정 0으로도 합리적 기본 동작 + 점진적 커스터마이즈",
        "children": [
          {
            "이름": "project",
            "역할": "로그/리포트 라벨",
            "구분": "config",
            "상세": ""
          },
          {
            "이름": "lockdown.files",
            "역할": "L0 잠금 파일",
            "구분": "config",
            "상세": "opt-in, 기본 0개"
          },
          {
            "이름": "enforcementFile / keywordsFile / severityMapFile",
            "역할": "규칙 파일 위치",
            "구분": "config",
            "상세": "없으면 번들 기본 fallback"
          },
          {
            "이름": "verify.checks[]",
            "역할": "검증 명령 선언",
            "구분": "config",
            "상세": "{id,cmd,timeoutMs,slow}"
          },
          {
            "이름": "lint.freshnessFiles[] · convergence · guides[] · ledger.staleSec",
            "역할": "기타 모듈별 대상 선언",
            "구분": "config",
            "상세": ""
          },
          {
            "이름": "dojo {defaultLang,stack,delegate}",
            "역할": "dojo 스캐폴더 기본 스택 (선택)",
            "구분": "config",
            "상세": "엔진 무하드코딩 — 선호 학습/커널 스택을 per-repo config 가 운반. delegate 설정+hexa 있으면 `hexa dojo <delegate>` 위임. 부재 시 제네릭 py stub"
          },
          {
            "이름": "docs.scratchDir = state",
            "역할": "산출물 단일 루트",
            "구분": "config",
            "상세": "실험·벤치·검증(verdict/claim)·스크래치 등 모든 작업 산출물을 repo-root `state/` 하나로 통일(c5). 하위 디렉토리 안 쪼갬 — `state/<id>` 평면(verdict 는 `state/<slug>/<id>.txt`). verdict/verify/atlas 가 이 경로를 공유. 재생성 가능 `build/` 만 .gitignore · 머신 자동로그는 `.harness/`"
          }
        ]
      },
      {
        "이름": "debt — 들어낸 비범용 부채 (교훈)",
        "역할": "원본 도구 일반화 시 제거한 안티패턴",
        "구분": "group",
        "상세": "이 도구에서 피할 것",
        "children": [
          {
            "이름": "경로 하드코딩",
            "역할": "절대경로 고정",
            "구분": "debt",
            "상세": "→ repo-root 상향 탐색 + env override"
          },
          {
            "이름": "L0 목록 하드코딩",
            "역할": "fallback 배열에 실제 파일 박힘",
            "구분": "debt",
            "상세": "→ config.lockdown (opt-in)"
          },
          {
            "이름": "도메인 모듈 번들",
            "역할": "배포/머니 모듈이 코어에 섞임",
            "구분": "debt",
            "상세": "→ ①/③ 등급 분리, ③ 제외"
          },
          {
            "이름": "SSOT 단일 repo 고정",
            "역할": "두 repo가 한 repo의 JSON에 수렴",
            "구분": "debt",
            "상세": "→ per-repo 로그, 공유는 sync로 명시"
          },
          {
            "이름": "검증 명령 하드코딩",
            "역할": "pnpm test 등 박제",
            "구분": "debt",
            "상세": "→ verify.checks[]로 선언"
          }
        ]
      },
      {
        "이름": "setup — 새 repo에 세우는 절차",
        "역할": "참조: install.md(통합), extending.md(확장)",
        "구분": "group",
        "상세": "",
        "children": [
          {
            "이름": "1. 엔진 추가",
            "역할": "git submodule add …/sidecar .harness-engine",
            "구분": "step",
            "상세": ""
          },
          {
            "이름": "2. config 작성",
            "역할": "cp harness.config.example.json harness.config.json",
            "구분": "step",
            "상세": "→ 수정"
          },
          {
            "이름": "3. (선택) 규칙 추가",
            "역할": "cp config/enforcement.json .harness/",
            "구분": "step",
            "상세": "→ repo 규칙 append"
          },
          {
            "이름": "4. hook 배선 (전역 1벌)",
            "역할": "sidecar install — 전역 ~/.claude/settings.json 에만 배선",
            "구분": "step",
            "상세": "GLOBAL-ONLY: per-repo .claude/settings.json 금지(전역과 중복돼 컨텍스트 2~3중 주입되던 버그). init 은 repo settings 를 안 쓰고, install-hooks --repo 는 거부. 훅은 호스트당 한 번 sidecar install 로 전역 배선 → 모든 repo 발사. (@convergence INIT-INJECT-DUP)"
          },
          {
            "이름": "5. 검증",
            "역할": "sidecar audit / sidecar ci list",
            "구분": "step",
            "상세": ""
          },
          {
            "이름": "6. 운영",
            "역할": "commit 전 verify, 세션 끝 ing, 규칙 추가 전 bitter-gate",
            "구분": "step",
            "상세": ""
          }
        ]
      },
      {
        "이름": "viewer — 아키텍처 뷰어",
        "역할": "이 트리(ARCHITECTURE.json)를 사람이 보는 도구",
        "구분": "group",
        "상세": "JSON이 SSOT, HTML/서버는 표시 수단",
        "children": [
          {
            "이름": "ARCHITECTURE.html",
            "역할": "의존성 0 자립형 뷰어",
            "구분": "viewer",
            "상세": "json fetch→컬럼 그리드 트리(접기·검색·다크모드·드롭 fallback)"
          },
          {
            "이름": "serve.py",
            "역할": "python3 serve.py [port] [--no-open]",
            "구분": "viewer",
            "상세": "로컬 정적 서버 + 브라우저 자동 오픈 — file:// fetch 차단 우회"
          }
        ]
      }
    ]
  }
}
