{"ts":"2026-06-27","title":"chore(commons): H-FIX-ROOT-CAUSE 에 '대체' 우회 시그널 추가","body":"- config/enforcement.json prompt_hint H-FIX-ROOT-CAUSE 에 '대체(?!로)' 추가(18→19) — 타-언어/도구로 갈아치우는 '대체' 우회 감지, '대체로'(일반적으로) 오탐 제외. #253 후속."}
{"ts":"2026-06-27","title":"feat(commons): fix-the-tool 규칙 + prompt_hint 강화 — 언어/도구 벽은 우회 말고 upstream-fix","body":"- commons 신규 규칙 fix-the-tool — 언어/도구 벽(예약어·파서갭·i64한계·stdlib부재·codegen·링크실패)은 우회 말고 그 도구를 직접 upstream-fix(전부 dancinlab repo). dont=타-언어 scratch 재구현·캐시바이너리·심볼회피·fallback·측정종착 조기선언으로 덮기(=wire-to-prod dead). prompt_hint H-FIX-ROOT-CAUSE 패턴 8→18 보강+힌트 명문화로 매턴 prompt-scan 표면화(기계강제). 동기=PCF 세션 i64/예약어 벽 우회 반복 사용자지적."}
{"ts":"2026-06-27","title":"feat(guard): GIT-BRANCH-SWITCH-MAIN — 메인 워크트리 브랜치 전환 차단(#3559 재발 방지) · 링크드 워크트리·파일복원 면제","body":"- 신규 코드레벨 가드 GIT-BRANCH-SWITCH-MAIN(modules/git-checkout-guard.ts): MAIN 워크트리에서 HEAD 이동하는 `git checkout <branch>`/`-b`/`-B`/`--orphan`/`--detach`/`-` 및 모든 `git switch` 차단 — 공유 primary 체크아웃을 갈아엎으면 병렬 세션의 untracked 작업이 날아가고 이후 커밋이 엉뚱 브랜치에 안착(#3559 parallel-worktree 사고 클래스).\\n- MAIN 한정: git-dir 에 `/worktrees/` 없음으로 판별 → 링크드/임시 워크트리는 면제(브랜치 전환이 격리 워크트리의 본 목적). 비-repo cwd 도 면제.\\n- 파일복원은 통과: `checkout -- <path>`, 2+ positional(`<tree-ish> <path>`)은 HEAD 미이동이라 무발화. 모호한 lone `checkout <ref>` 만 `git rev-parse --verify <ref>^{commit}` 으로 실제 ref 확인 후에만 차단(`checkout README.md` 같은 파일복원은 rev 실패 → 통과).\\n- 인라인 override 없음(force-push 가드와 동일 — 공유상태 보호) · config `git.guardBranchSwitch` 토글 기본 ON. 후보 파싱됐을 때만 git 호출(per-bash latency 0).\\n- QA: 파서 단위 18/18 PASS · e2e(실제 cd) MAIN checkout-branch/-b/switch=BLOCK · MAIN file-restore/status·LINKED-wt·non-repo=ALLOW · tsc clean."}
{"ts":"2026-06-27","title":"fix shadow","body":"shadow regen orphan-prune — 소스 commands/*.md 에 없는 marker-shadow(폐기된 명령)를 ~/.claude/commands 에서 자동 제거(손수작성은 보존). 기존엔 regen 이 추가만 해서 폐기 명령의 bare /cmd 가 picker 에 잔존(kick 폐기에서 발견). plan 은 would-prune 만. 검증: 실제로 orphan 3개(convergence·kick·poll) 청소"}
{"ts":"2026-06-27","title":"chore kick","body":"kick 플러그인 폐기 (bitter-gate retire) — hexa kick/drill 패스스루 명령 제거: modules/kick.ts + commands/kick.md 삭제 · cli/index.ts import·HELP·dispatch(kick/drill) 제거 · toolkit ALIASES drill→kick 제거 · ARCHITECTURE module-kick-alias-drill 노드 제거 · gap.md/README 의 /kick 참조 제거. toolkit 75→74 sync · 게이트 lint ok. shadow 재미러로 bare /kick 제거"}
{"ts":"2026-06-27","title":"refactor architecture","body":"ARCHITECTURE 트리 노드 스키마를 영어 canonical 키로 통일 — slug→id + 한글키 이름/역할/상세→name/role/detail (models/datasets/convergence 가 이미 id 키 → 정합). 게이트 ARCH-SLUG-MISSING/DUPE/FORMAT→ARCH-ID-* (severity-map ×2). 레거시 키(slug·이름·역할·상세)는 read-fallback(nodeKey/nodeName/nodeRole/nodeDetail)로 용인 — 마이그레이션 중·타 repo 트리가 깜깜해지지 않게. architecture.ts(타입·skeleton·search·lint)·init.ts 스캐폴드·ARCHITECTURE.html fallback·cli HELP·README·자기 ARCHITECTURE.json 전 노드 전환. 검증: id-dupe block 강제·레거시 한글노드 read+search OK·architecture lint ok. L0 cli/index.ts HELP 1줄 의도적 편집(# no-verify-ok)"}
{"ts":"2026-06-27","title":"fix(self-update): non-destructive updateClone — stop wiping unmerged local commits on reset","body":"self-update updateClone 을 NON-DESTRUCTIVE 로 — blind reset --hard origin/main 이 클론의 미머지 로컬 커밋을 매번 소멸시키던 root-cause 차단. fetch 후 dirty 또는 origin/main 보다 ahead 면 reset 거부(skipped) → selfUpdate 가 clobber 대신 warn(로컬 작업 보호 · main 머지/push 후 재실행). 머지로 ahead=0 되면 재개. model.ts add() 의 dead k===sha ternary 제거 + setup.ts res 타입에 skipped? 추가 → tsc clean. origin/main 머지(reset no-op)와 이중 방어."}
{"ts":"2026-06-27","title":"feat(pod): sidecar pod {poll|watch|unwatch|list} — GPU pod 자동 폴링 명령","body":"sidecar pod {poll|watch|unwatch|list} 추가 — GPU pod 자동 폴링(>=10분 cadence)을 canonical 명령으로 일반화. 모든 원격 op은 hexa cloud 경유(alive/exec/copy-from/rm · 직접 ssh/curl 금지). poll=one-shot read-only(teardown/pull은 명시 플래그만 · pull-then-destroy a_fire_recover_complete). watch=레지스트리(~/.sidecar/pod-watch.json)+cadence: --cron(시스템 crontab) 또는 agent ScheduleWakeup fallback(기본 비파괴, macOS crontab write 권한 없을 때 자동 fallback). modules/pod-poll.ts 신규 + runbooks.ts runPod 디스패치 + index.ts HELP + commands/pod.md + TOOLKIT.jsonl 동기."}
{"ts":"2026-06-27","title":"feat architecture","body":"architecture inject 를 풀-덤프(43.8KB)에서 스켈레톤(title+summary+2단계 목차 · ~5KB)+온디맨드 Read 로 교체 — 대용량 정적-doc canonical 패턴(세부는 show/search/Read pull). 매 UserPromptSubmit 재주입 제거(정적 doc=SessionStart+Compact 1회 · 매턴 행동규칙 inject 만 per-turn 유지) — hooks.json·setup.ts·전역 settings.json 정렬. setup.ts 누락이던 Stop 훅 architecture stop-check 추가(턴마감 게이트 결정적 강제 · hooks.json 과 정렬). ARCHITECTURE module-architecture 노드 갱신 + 리서치 학습 convergence INJECT_DOC_SKELETON_ONDEMAND 기록"}
{"ts":"2026-06-27","title":"feat(pool): 'pool status' now shows live ⚡CPU·RAM·GPU occupancy too (one probe = reachability + load) — stop missing GPU usage that only 'pool list' surfaced","body":""}
{"ts":"2026-06-27","title":"refactor(dataset): dataset.ts 를 복구된 model.ts 미러로 재정렬 (공통 splice writer·flag 파서·lib/log idiom)","body":"dataset.ts 를 복구된 model.ts(dangling ee58010)와 동일 구조로 재작성 — 공통 idiom: lib/log(info/ok/loudFail) · flags() 파서 · findArrayEnd string-aware bracket matcher · save() splice(rows sort + models 블록 옆 인접 삽입) · rm --yes 가드. 기능 동등(list+--lang/--register 필터+4칸 그리드·show·add·set·feat·rm). model⇄dataset 두 레지스트리가 byte-불변 top-level splice·flag 파서를 공유해 유지보수 평행. commands/dataset.md 도 model.md prose 포맷으로 미러. ARCHITECTURE/README dataset 노드에 mirror 명시."}
{"ts":"2026-06-27","title":"feat(dataset): sidecar dataset registry → ARCHITECTURE.json .datasets[] (byte-불변 splice · 4칸 lang×register)","body":"feat(dataset): sidecar dataset registry — ARCHITECTURE.json top-level .datasets[] (모델 레지스트리의 데이터-짝). modules/dataset.ts: list/show/add/set/feat/rm + --lang/--register 필터, 4칸 lang×register 그리드. writer=byte-불변 top-level splice(datasets 블록만 교체, 나머지 트리 byte-동일 → JSON 전체 re-stringify 회피). cli/index.ts dispatch+HELP, commands/dataset.md(SHADOW-DESC clean), TOOLKIT.jsonl 동기, ARCHITECTURE.json/README modules 행 추가."}
{"ts":"2026-06-27","title":"fix(errors): severity-map 로드를 bundled defaults + repo override 병합으로 — 새 bundled 규칙이 stale per-repo 맵에서 fallback-block 둔갑하던 결함 차단 (warn 규칙이 어디서나 warn)","body":""}
{"ts":"2026-06-27","title":"fix(architecture): ARCH-SCHEMA-UNRECOGNIZED — slug 게이트 silent no-op 표면화 (이름-노드 0 = off-schema 설계문서)","body":""}
{"ts":"2026-06-27","title":"feat(pool): live RAM occupancy in 'pool list' ⚡badge (CPU·RAM·GPU)","body":"pool list's live ⚡badge now includes system RAM occupancy alongside CPU and GPU. LOAD_PROBE gained a POSIX-sh RAM segment: Linux reads /proc/meminfo (used = MemTotal − MemAvailable), macOS falls back to vm_stat free+inactive × hw.pagesize vs hw.memsize. probeLoad parses RAM=<usedMiB,totalMiB>; fmtLoad renders 'RAM <pct>%·<used>/<total>G' between the CPU and GPU segments. status/specs still show static capacity only — live occupancy is list-only."}
{"ts":"2026-06-27","title":"feat(changelog): auto-prune to keep-N (add-time + SessionStart autoprune) + fix(pr-cycle): explicit --head + worktree-aware base sync","body":"Two follow-ups surfaced by the cross-repo CHANGELOG sweep. (A) changelog auto-prune: 'add' now auto-trims the working file to keep-N (config lint.changelog.keep, default 30) after appending, and a new 'autoprune' verb (SessionStart-wired via setup.ts) trims to keep-N when over cap (silent no-op under cap); full history stays in git. (B) pr-cycle fixes: gh pr create now passes explicit --head <branch> (worktree head-inference quirk), and the post-merge local-base ff-sync now reports a benign skip (not a misleading warning) when <base> is checked out in another worktree."}
{"ts":"2026-06-27","title":"feat(changelog): CHANGELOG.md → CHANGELOG.jsonl (append-only JSONL) + prune command","body":"Owner: 'CHANGELOG를 jsonl로 바꾸고 오래된건 삭제 명령어로'. The append-only history moved from a freeform CHANGELOG.md to CHANGELOG.jsonl (newest-first JSONL, {ts,title,body}) so entries are machine-parseable and prunable per-entry. New modules/changelog.ts: add(append, body via stdin, ts=today) / list / render(markdown view) / prune(--keep N | --older-than D, deletes old entries) / migrate(md->jsonl, ran: 240 entries lossless). Rewired the CHANGELOG-MISSING gate everywhere it read CHANGELOG.md -> CHANGELOG.jsonl: harness.config.json lint.changelog.file, pr-cycle doc-gate regex+msg, init.ts scaffold, commons single-doc, CLAUDE/README/ARCHITECTURE docs. cli register + help + commands/changelog.md slash. Old CHANGELOG.md removed."}
{"ts":null,"title":"chore(ing): swap the ING status marker 🔵 → 🔄 (in-progress glyph)","body":"Owner picked 🔄 (in-progress/refresh) over the blue circle as the ING anchor. Swapped the marker everywhere\nthe ING enforce reads/writes it: the `ing inject` board header + turn-close directive, the `ing stop-check`\ndetection regex (`/🔵 ING/` → `/🔄 ING/`), and the ARCHITECTURE/README/commons docs that describe it. So the\nrequired per-turn line is now `🔄 ING 갱신: …` / `🔄 ING: 변동 없음`. The verify-tier 🔵 (SUPPORTED-FORMAL badge\nin `verify.ts`/templates) is a different semantic and was left untouched."}
{"ts":null,"title":"feat(architecture): stop-check — enforce a 🏛️ ARCHITECTURE report when code/structure changed","body":"Companion to `ing stop-check`. ING is enforced EVERY turn (progress can shift invisibly); design changes are\nrarer, so the architecture enforce is CONDITIONAL — it must not demand a 🏛️ line on read-only turns.\n\n- **`architecture stop-check`** (new Stop hook · `decision:block`) — fires only when the working tree has\n  uncommitted code/ARCHITECTURE changes (deterministic `git diff` trigger) AND the last response carries no\n  `🏛️ ARCHITECTURE` line; blocks to force the agent to update the design tree in lockstep or affirm\n  `🏛️ ARCHITECTURE: 변동 없음`. Clean tree → no-op. Catches code↔ARCHITECTURE drift (commons cycle-docs-pr).\n  sidecar-managed repos only; `stop_hook_active` loop-guard.\n- turn-close `🏛️ 턴 마감 게이트` directive rewritten to require one of the two lines when code/structure\n  changed; wired into `hooks/hooks.json` Stop + global `~/.claude/settings.json`."}
{"ts":null,"title":"fix(pr-cycle): fall back to `main` when the remote HEAD symref is unset","body":"The doc-gate base was `git rev-parse --abbrev-ref origin/HEAD` stripped of `origin/`, with a\n`|| \"main\"` fallback. But repos created by scaffold (not `git clone`) never get a remote HEAD\nsymref, so the command returns the literal `origin/HEAD` → strips to `\"HEAD\"` — non-empty, so\nthe fallback never fired. The gate then diffed `origin/HEAD...HEAD` (a bogus base), saw a tiny\nwrong change set, and fired a misleading \"문서 업데이트 필수 — CHANGELOG/ARCHITECTURE/README\n누락\" even when those docs WERE updated.\n\n- **modules/pr-cycle.ts** — treat a stripped result of `\"HEAD\"` (or empty) as \"no default\n  known\" and fall back to `main`; a real non-main default branch still passes through.\n- Verified the branch logic for `HEAD`/empty/`main`/`develop` inputs. (Sibling scaffold repos —\n  `carbon-capture`, `lumen`, `rtsc`, `echoes` — all lacked the symref; `git remote set-head\n  origin main` fixes a repo, and this hardens the tool so an unset symref no longer misleads.)\n  (Typecheck CI `npx tsc` is unavailable in this environment; change is type-trivial,\n  `node --check` parses clean.)"}
{"ts":null,"title":"fix(paths): a `.git` dir is a hard boundary for REPO_ROOT discovery","body":"`findRepoRoot()` walked the whole ancestry for a `harness.config.json` BEFORE falling back to the\nnearest `.git`, so a config in a non-git parent could hijack REPO_ROOT and break every git call.\n\nHit in `dancinlab/`: sibling repos (`carbon-capture`, `lumen`, `rtsc`, `echoes`) each have their own\n`.git` but no per-repo `harness.config.json`, while the parent monorepo `dancinlab/harness.config.json`\nexists and is NOT a git repo. From any sibling, `REPO_ROOT` resolved to `/Users/.../dancinlab`, so\n`pr-cycle` ran git there → `fatal: not a git repository`, mis-counted the change set, and the doc gate\nfired spuriously. (`ing` was unaffected — it resolves git independently.)\n\n- **lib/paths.ts** — `findRepoRoot()` now returns the current dir as soon as it has EITHER a\n  `harness.config.json` (checked first, so an in-dir config still wins) OR a `.git` directory; it never\n  ascends past a git root. Dropped the `firstGit` deferral. Discovery-order comment updated.\n- Verified: from `carbon-capture` and `lumen`, `REPO_ROOT` now resolves to each repo's own root (was the\n  non-git parent); `~/.sidecar/cli` and the monorepo root itself are unchanged; a deep subdir climbs to its\n  `.git` boundary. End-to-end, `pr-cycle` from a sibling repo now reaches its real branch/doc logic.\n  (Typecheck CI `npx tsc` is unavailable in this environment — no global/local typescript; change is\n  type-trivial and `node --check` parses clean.)"}
{"ts":null,"title":"feat(ing): per-turn ING-status enforce — every response must carry a 🔵 ING line (Stop gate)","body":"The turn-close ING directive was advisory (\"update the board if progress changed, else skip\"), and the only\nhard signal was a code-edit counter (`ing staleness-check`, warn-only). But progress changes WITHOUT a code\nedit too — a measurement, a verdict, a bench result, a background-agent landing — so an edit-counter misses\nthose turns. Owner asked to enforce an ING update every turn.\n\n- **`ing stop-check`** (new Stop hook · `decision:block`) — mirrors `recommend stop-check`: reads the last\n  assistant text and blocks the turn if it carries no `🔵 ING` line, forcing the model to emit one. NOT tied\n  to file edits (captures measurement/verdict/agent-landing progress); the agent consciously reports either\n  `🔵 ING 갱신: <what>` (board mutated) or `🔵 ING: 변동 없음` (no-change). Scoped to sidecar-managed repos\n  (harness.config.json); native `stop_hook_active` loop-guard caps it at once per chain.\n- **turn-close directive** (`ing inject`) rewritten: every response must include one of the two `🔵 ING`\n  lines; progress explicitly includes non-code signals.\n- Wired into the plugin `hooks/hooks.json` Stop array and the global `~/.claude/settings.json`.\n- `ing staleness-check` (code-edit warn) stays as a secondary signal."}
{"ts":null,"title":"chore(changelog+styles): retire the user-request quote convention + explicit slug-block severity","body":"The owner asked to drop the speaker-emoji user-request quote line that CHANGELOG entries had been opening\nwith, and to check the plugin wasn't *forcing* it. Audit: no hook/lint/template hard-mandated it — the only\nsoft nudge was the easy response-style listing \"commit-message body user-summary section\" as a Tier-A scope,\nwhich pulled friendly user-summary prose (and the habitual user-request quote) into commit/CHANGELOG bodies.\n\n- **easy styles** — removed the \"commit-message body user-summary section\" scope line from all five\n  `styles/easy*.md` (en/ko/ja/zh/ru). Easy prose no longer applies to commit bodies, so nothing nudges a\n  user-summary/quote there. (Self-update propagates the global `~/.sidecar/cli/styles/` copies.)\n- **CHANGELOG** — scrubbed all 51 historical user-request quote lines; the convention is retired, not\n  authored going forward.\n- **severity-map** — mapped `ARCH-SLUG-MISSING` / `ARCH-SLUG-DUPE` / `ARCH-SLUG-FORMAT` to explicit `block`\n  (was fallback-block only), so a slug-less / dup / malformed-slug tree node is a first-class hard block at\n  commit time (git hook + global settings.json commit-lint gate). Verified: missing slug → `[block]`, exit 1."}
{"ts":null,"title":"feat(lab): scaffolded CLAUDE.md documents the hexa-lang stdlib + QFORGE conventions","body":"`/lab init`'s generated `CLAUDE.md` now carries two demiurge cross-repo conventions up front,\nso every new lab repo states them from day one (lifted from `demiurge/CLAUDE.md` d3 / d_qforge_*):\n\n- **hexa-lang stdlib** (demiurge d3) — implementation code lives in the canonical `hexa-lang`\n  stdlib; this repo *consumes* it and never owns stdlib. Topical/domain folders hold docs /\n  manifests only — no duplicated implementation, not a code home. Reusable logic → upstream.\n- **QFORGE compute default** (demiurge d_qforge_default) — heavy compute (DFT/DFPT/el-ph/sim)\n  defaults to the from-scratch `QFORGE` stack, not QE/bespoke; QE is reference/fallback for\n  un-migrated pieces only, migrate piece-by-piece behind a ≤1 % gate, decks via `hexa deck`.\n  Marked *(skip for non-compute labs)*.\n- `tool/` gotcha clarified: thin verification harness only, not a home for reusable impl."}
{"ts":null,"title":"feat(lab): `/lab init` — scaffold the lumen/rtsc/carbon-capture sibling-repo skeleton","body":"The lumen / rtsc / carbon-capture sibling repos all share one skeleton (src/ · state/ · ARCHITECTURE.json +\narchitecture.html viewer + serve.py · CLAUDE/README/CHANGELOG · HYPOTHESES/ pre-register→falsify→run→verdict ·\ntool/<slug>.py shared harness). It was hand-rebuilt each time. `/lab init` mints it in one shot.\n\n- **`lab init [dir] [--force] [--dry-run] [--name N] [--desc D]`** (`modules/lab.ts` → `runLab`, dispatched\n  in `cli/index.ts`) — recursively renders the tokenized skeleton in `templates/lab/` (`{{NAME}}` /\n  `{{SLUG_SNAKE}}` / `{{DESC}}` / `{{DATE}}`, applied to file CONTENTS and the `tool/{{SLUG_SNAKE}}.py` path)\n  into a target repo. Never overwrites an existing file unless `--force`; `--dry-run` reports only. Name\n  auto-detects from `basename(dir)` (override with `--name`); default target = cwd. Files-only — no git\n  init/push (outward-facing git stays out of scope, matching `init`). Empty dirs kept via `.gitkeep`.\n- **`commands/lab.md`** delegator (shadowed to bare `/lab`); ARCHITECTURE.json + TOOLKIT.jsonl entries added.\n- Verified: dry-run lists 15 files writes nothing · real scaffold renders `tool/<slug>.py` · valid JSON ·\n  serve.py parses · harness imports · no unrendered tokens · non-clobber + `--force` both correct."}
{"ts":null,"title":"feat(pre): global commit-lint gate — enforce the lint gate via settings.json, not a per-repo git hook","body":"The lint commit-gate lived ONLY in a per-repo `.git/hooks/pre-commit` (installed by `sidecar init`), so a\nrepo that was never init-ed (e.g. anima) committed completely un-gated — its 89 oversized ARCHITECTURE\ncells never blocked a commit. Since `sidecar pre bash` is already wired GLOBALLY via `~/.claude/settings.json`\nPreToolUse(Bash), an agent `git commit` can be intercepted there to run the SAME lint everywhere.\n\n- **`pre.ts` commit interceptor** (`COMMIT-LINT`) — detects an agent `git commit`, and in a sidecar-managed\n  repo (harness.config.json present) runs the lint gate, blocking on any block-severity violation with the\n  list + fix hint. `git commit -a/-am/--all` widens the lint scope to the auto-staged tracked-modified set\n  (precise `-a` detection, no over-match → no false block). Honors the same `--no-verify`/`# no-verify-ok`\n  escape the danger guard governs (one consistent opt-out, not a new hatch).\n- **`lint.ts` refactor** — extracted `collectViolations()` (pure, no printing) + `lintBlockers()` (block-severity\n  filter) from `runLint`, exported `Violation`. The git pre-commit hook and the global interceptor now share\n  ONE lint implementation (native-canonical-first, no reinvent).\n- Net: the enforce gate is now global — every sidecar-managed repo (anima included) gets agent-commit lint\n  enforcement from the shared settings.json, no per-repo `sidecar init --hooks` needed. The per-repo git hook\n  still covers non-agent (human/script) commits."}
{"ts":null,"title":"feat(architecture): retire 구분/type field → searchable per-node slug + `architecture search`","body":"The ARCHITECTURE.json tree nodes carried a `구분` (type/category) column that wasn't grep-friendly and\nthe owner found dispensable. Replaced it with a stable, searchable `slug` per node + a search verb:\n- **Schema migration** — every tree node (126) now has a unique kebab-case `slug`; the `구분` field is\n  dropped and its category folded into the slug prefix (`force-push 가드` → `guard-force-push`, `ing` →\n  `module-ing`, …) so zero category info is lost and `search guard`/`search module`/`search config` work.\n  `columns` 구분→slug. The HTML viewer is column-driven, so it renders + searches the slug column with\n  no viewer change.\n- **`sidecar architecture search <query>`** — case-insensitive substring over slug / 이름 / 역할 / 상세,\n  prints each matching node's slug + breadcrumb path. Korean queries still match via 이름/상세 text.\n- **slug lint** — `lintArchitectureTree` now emits `ARCH-SLUG-MISSING` / `ARCH-SLUG-DUPE` /\n  `ARCH-SLUG-FORMAT` (block via severity-map fallback), so every node stays addressable and slugs stay\n  unique kebab-case. `init.ts` scaffolds the slug schema for new repos."}
{"ts":null,"title":"feat(commons): retire the poll discipline — drop poll-min-30m/poll-max-10m rules, both guards, the /poll runbook","body":"a flapping host, capping check frequency), so the owner asked to remove it entirely.\n\nRemoved the whole poll-discipline subsystem (was c19 `poll-min-30m` + c21/c22 `poll-max-10m`):\n- **commons rules dropped** — `poll-min-30m` (poll external long-runners ≥30min) and `poll-max-10m`\n  (check live long-runners ≥10min) deleted from `config/commons.md`.\n- **both enforcement guards deleted** — `modules/poll-guard.ts` (POLL-INTERVAL block on short bash poll\n  loops) and `modules/heartbeat-guard.ts` (POLL-HEARTBEAT stale-long-runner warn). Unwired from\n  `pre.ts` (POLL-INTERVAL), `post.ts` (heartbeat stamp/warn + bg-launch auto-track), `ing.ts` (live\n  marker + heartbeat warn — `ing pod add/rm` bookkeeping kept), and `lib/config.ts` (`poll.maxSilenceSec`).\n- **/poll runbook retired** — `commands/poll.md`, `templates/poll.md`, `runbooks.ts::runPoll`, the\n  `cli/index.ts` import/help/dispatch, and the ARCHITECTURE poll-guard/heartbeat/runbook cells +\n  the two convergence records (`NO_SHORT_POLL_LOOP`, `NO_ABANDONED_LONGRUNNER`). README + marketplace\n  guard lists and the ci-track `c19` parentheticals scrubbed of the now-dead rule ids.\n- Lean-trimmed the pre-existing over-cap `ci` ARCHITECTURE cell (743→<300 chars · surfaced by the\n  ARCH-BIG-CELL lint while shipping this) to keep the design tree green.\n\n`ci-track --watch` (remote PR/CI polling) is a CLI feature, not the poll discipline — it stays."}
{"ts":null,"title":"feat(ci): scaffold cost-free fast CI path — self-hosted pool ▸ github-hosted fallback + warm cache (no Blacksmith)","body":"`sidecar ci scaffold` 가 만드는 `.github/workflows/ci.yml` 을 fast-free 구조로 업그레이드.\n기존엔 단일 잡 `runs-on: ${ci.runner}`(기본 ubuntu-latest)뿐 — pool 우선·fallback·캐시 없음.\n이제 config `ci.fallback`/`ci.cachePaths`(둘 다 optional·미설정=기존 단일잡 그대로=하위호환):\n\n- **`ci.fallback`** 설정 시 → `pick-runner` dispatch 잡(무료 github-hosted·timeout 2분)이\n  `gh api .../actions/runners` 로 self-hosted pool 의 ONLINE+idle 러너를 probe →\n  있으면 `ci.runner`(self-hosted·public repo 무료 12-core), 없으면 `ci.fallback`(무료 github-hosted).\n  `verify` 잡은 `runs-on: ${{ fromJSON(needs.pick-runner.outputs.runs_on) }}`.\n  **safe-by-construction**: probe 에러(PAT 부재 403·offline) → fallback 이라 CI 가 영구 큐대기 안 함.\n  pool 선호를 실제 켜려면 repo secret `RUNNER_PROBE_TOKEN`(admin:read PAT) — 없으면 안전하게 무료 fallback.\n- **`ci.cachePaths`** 설정 시 → checkout 뒤 `actions/cache@v4` 스텝(`key=os-proj-ci-<sha>` +\n  `restore-keys` warm 재사용). ccache/툴체인/deps 따뜻하게 재사용 = 반복 빌드 가속.\n- `runnerToJson()` 헬퍼: `[self-hosted, Linux, X64]` YAML 리스트 ↔ JSON 배열 ↔ 단일 라벨 문자열을\n  `fromJSON()` 한 출력으로 통합. `serializeStep` 은 멀티라인 `with` 값(예: cache `path`)을 YAML 리터럴 블록(`|`)으로 emit.\n\n근거: GitHub larger runner 는 public repo 도 per-minute 과금(Blacksmith 와 동일 함정), 표준/self-hosted 러너만\npublic repo 무료(GitHub docs). 따라서 \"Blacksmith 없이 최대한 빠른 CI\" = self-hosted pool 우선 + 무료 fallback + 캐시.\n설계 결정을 일회성 repo 가 아니라 **ci 플러그인 표준**으로 박아 전 repo 전파(root-cause).\n\n검증: `ciWorkflowYaml` 두 변종(no-fallback 하위호환 / fallback+cache) 렌더 → python `yaml.safe_load` 양쪽 OK ·\n`runnerToJson` 3 케이스 ✓ · `cli help` rc=0 로드."}
{"ts":null,"title":"feat(research): web 검색 + fetch 페이지 서브커맨드 — Claude Code WebSearch/WebFetch 키리스 대응","body":"- Claude Code 의 WebSearch/WebFetch 는 세션 내장 도구라 갖다 붙일 공개 CLI API 가 없음 → arxiv·yt 와 동일한 **키 없는 자체 구현**으로 같은 결과를 냄. `research web <query> [--n N]` = DuckDuckGo lite 키리스 검색(title/url/snippet 행) · `research fetch <url>` = 페이지 GET → script/style 제거 후 HTML→읽기텍스트(15k 캡). 둘 다 어떤 에이전트 런타임에서도 쓰는 결정적 CLI. 별칭 `search`/`url`. 네트워크 의존·오프라인 graceful. 라이브 스모크: alphafold 검색 3건 · example.com fetch OK."}
