# Design

Sidecar's live design rules: [`project.tape`](project.tape) — the `@D s1..s5` governance blocks (concept separation · structured carriers · portable scripts · commons carrier · project.tape carrier) + `@D ship` (ship cycle).

Cross-project rules: [`hooks/commons/commons.tape`](hooks/commons/commons.tape) — `@D g1..g29`.

Decision audit trail (one decision per gate, never batched): [`DESIGN.log.md`](DESIGN.log.md).
