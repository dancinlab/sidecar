# INBOX — current state

@goal: cross-project handoff 수신함 — 다른 repo가 sidecar로 넘긴 gap·patch·note를 추적하고 해소

(현재 상태만 기록 — 열린 handoff는 `- [ ]` 로, 처리 이력은 `INBOX.log.md` 로)

- [ ] **`pool` CLI 회귀 — 모든 subcommand 미실행, `OK: <첫-인자>` stub 만 출력** (from hexa-lang RUNTIME 2026-05-26) — `pool on <host> <cmd>` · `pool list` 등 **전 subcommand 가 명령을 실행하지 않고** `OK: <첫인자>` 한 줄만 반환(`pool on mini …`→`OK: on`, `pool list`→`OK: list`). bare `pool` → **hexa-cc usage** 출력(`Usage: hexa-cc <input.hexa> <output.c>`) = `hexa <pool.hexa>` 가 컴파일러로 잘못 디스패치되거나 설치본 stub. **세션 중 회귀**: 같은 세션 초반 heavy 빌드(`pool on mini bash -s < script`, hexa-lang aprime_cc 검증)는 **정상 작동**했고 중간(~PR#1237 직후)에 깨짐 → 동시 에이전트 reinstall/clobber 정황. shim(`~/.hx/bin/pool`, 772B)은 정상(`exec "$HEXA" ~/.hx/packages/pool/bin/pool.hexa "$@"`); 1순위 HEXA 후보 `$HOME/Dev/airgenome/nexus/shared/bin/hexa` 는 **부재**라 skip→`~/.hx/bin/hexa` 사용 → 따라서 `~/.hx/bin/hexa`(인터프리터가 hexa-cc 로 바뀜 의심) 또는 `~/.hx/packages/pool/bin/pool.hexa` 설치본 손상이 원인 후보. **영향**: g9 pool offload 전면 불가(Mac/GPU/heavy-build 디스패치). **workaround**: 직접 `ssh <host> 'bash -s' < script` 작동 확인(검증 계속 진행 중). **분류**: pool sibling repo(`dancinlab/pool`) install 또는 hexa-resolution. 재현=`pool list` → `OK: list`.

- [x] **`/micro-exp` (context-driven sweep) + commons.tape g63-g66** ✅ (from demiurge RTSC 2026-05-25) — skill 0.2.0 **context-driven 재설계** land (manifest 파일 없이 `/cycle`처럼 맥락에서 candidate self-enumerate · 바=현재 맥락·인자=스코프) + commons.tape **g63 honest-sweep · g64 budget-cap · g65 aggregation · g66 pod≠agent-cap** land (유저 `sidecar sign commons` 후). 상세 → `INBOX.log.md`.
- [x] **worktree/branch 하네스 4-gap** (from anima 2026-05-25) — sidecar 몫 **#4(pr-cycle `gh pr merge` cross-repo 오작동) ✅ 해소** (pr-cycle 0.3.6 — `--repo` 파싱→merge 전파 · cross-repo 시 로컬 worktree cleanup skip). 남은 **#1**(공유 .git HEAD main 이동)·**#2**(로컬 브랜치 ref 유실)·**#3**(worktree agent PR 무관파일 bundle)은 **harness-upstream**(Anthropic `isolation:worktree` 영역) — sidecar 직접 fix 불가, 추적만. 상세 → `INBOX.log.md`.
- [ ] **worktree-gc 가 활성 사용 중 worktree 를 prune (브랜치명 재사용 → "origin-gone" 오판)** (from demiurge monograph fan-out 2026-05-26) — `hooks/worktree-gc` 0.1.0 이 *작업 중* `wt-*-mono` worktree 를 mid-task wipe (`.git` 링크 + `main.tex`/`Makefile`/`appendix/` 소실 · `companion/`+`cover.png` 만 생존 = 비원자 prune). 원인 추정: 이전 세션이 동명 브랜치(`feat/cern-monograph`)를 merge→origin 삭제 → GC 가 브랜치명 기준 "origin-gone/merged" 오판 → 동명 재사용한 신규 worktree(미푸시 라이브 작업)를 파괴. `open-PR skip` 가드는 빌드 단계(PR 생성 전)엔 무력. fix 후보: dirty/recent-mtime 가드 · HEAD-ancestor(진짜 merged) 체크 · cwd-in-use 체크 · 원자 prune. CERN(#220)·ANTIMATTER(#222) 둘 다 발생(checkpoint-commit 으로 복구). 상세 → `INBOX.log.md`.
- [x] **pool.json roster race-wipe 방어** ✅ (from demiurge RTSC 2026-05-25) — **pool 0.8.5 에서 해소** (sibling repo `dancinlab/pool` · `601a42d`). `_save` 비원자 truncate-rewrite 가 원인이었고 세 겹 방어로 land: **원자 write**(temp→`mv -f` rename(2)) + **empty-clobber 가드**(살아있는 roster 를 빈 것으로 덮으면 거부 · `pool rm` 만 예외) + **.bak 로테이션**(+ `_load` 빈/torn 파일 시 `.bak` 복구). 30-writer 동시 storm 무손상 검증 · `hx install pool` 로 로컬 라이브. 제안 5종 중 **flock 직렬화는 deferred**(macOS `flock(1)` 부재 · 원자 write 가 관측된 wipe 를 이미 제거 · lost-update 는 저severity). 상세 → `INBOX.log.md`.
- [x] **`/domain` 아이콘+별칭 타이틀/서브타이틀** ✅ (from anima IIT4 2026-05-25) — **domain 0.8.5 에서 land**. 옵션 `@title:` 필드 + `/domain title <text>` (alias `subtitle`) 서브커맨드 + bare `/domain`·`set` 출력 렌더(`@title:` 있으면 `◆ 🧠 IIT4 — "의식 측정자(尺)"   🎯 <goal>`, 없으면 현행 plain). non-breaking(미설정=현행 동작) · lint 은 `@title:` 부재 무경고(취향 필드). easy plugin 7요소와 도메인 트래커 정합 갭 해소. 상세 → `INBOX.log.md`.
- [ ] **pool data-locality + sign-local 토큰 < build 시간** (from anima PURE 2026-05-25) — local-data 의존 `hexa run`(`--corpus-path` 등 Mac-로컬 입력) 두 갈래 다 막힘: (a) bare `hexa` → pool-route ubu-2 → 입력파일 미동기화 → `source not found`(compile-stage, data 부재인데 code 오류처럼 보임), (b) 절대경로 `hexa` → fork-storm `sign local` 게이트인데 **토큰 5분 < 실제 build 10-20분** → build 중간에 재차단. F-CURRICULA-1 fire 가 이 조합으로 2회 BLOCKED. pool-route 0.6.10 local-pin 위에 남은 갭. 상세 → `INBOX.log.md`.
- [x] **g61 stdlib-SSOT 강제·자동화·범위확장** ✅ (from anima IIT4 2026-05-25) — 4/4 모두 land: (1) **g61 범위확장** commons 0.10.7 (primitives→+domain engines · engine⊥adapter · import-root) `30620ee`, (2) **stdlib-ssot-guard 0.1.0** hook (PreToolUse 비차단 advisory: anima-locked import + stdlib dup-fn · SessionStart stdlib-root 검증, guard 4-test PASS), (3) **stdlib 0.1.0** skill (`/stdlib check`·`promote` 런북), (4) import-root = SessionStart 체크. user `sidecar sign commons` 후 g61 land. 선행 사례: anima IIT4 → stdlib/consciousness/iit4 (hexa-lang #1051) + anima shim (#542) + hexa-brain EEG adapter (#1). 상세 → `INBOX.log.md`.
