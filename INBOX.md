# INBOX — current state

@goal: cross-project handoff 수신함 — 다른 repo가 sidecar로 넘긴 gap·patch·note를 추적하고 해소

(현재 상태만 기록 — 열린 handoff는 `- [ ]` 로, 처리 이력은 `INBOX.log.md` 로)

- [ ] `/domain` folder-nested domain 미지원 — `<NAME>/<NAME>.md` 중첩 SSOT인데 `/domain set`이 repo-root `<NAME>.md`만 찾아 빈 스캐폴드 재생성 + `/cycle`(g58) milestone 열거 실패 — from demiurge CARDIO+ (상세 `INBOX.log.md`)
