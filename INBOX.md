# INBOX — current state

@goal: cross-project handoff 수신함 — 다른 repo가 sidecar로 넘긴 gap·patch·note를 추적하고 해소

(현재 상태만 기록 — 열린 handoff는 `- [ ]` 로, 처리 이력은 `INBOX.log.md` 로)

- [x] **sbs 0.6.0 자동 QA 4축** ✅ (from user 2026-05-28) — handoff agent의 ship 직후 자동 4축 QA (functional·visible·conformance·regression) · regression FAIL → auto-revert · 나머지 alert + plan.md `## qa-deferred` · SKIP=PASS-equivalent. 상세 → `INBOX.log.md`.

- [~] **`/micro-exp` ↔ `/cycle-bg` build/fire phase 경계 + meta-domain 패턴** (from hexa-codex CODEX 2026-05-28) — PARTIAL (① + ⑤ ✅ shipped 2026-05-28 · ② deferred-sign · ③ absorbed into ② hint + ① 일반화 · ④ deferred-docs) — `/micro-exp 22-axis-candidate first-probe sweep` 가 22 candidate matrix (`AXIS.easy.md` 의 ⭐⭐⭐ 12 + ⭐⭐ 10 CODEX milestones) enumerate 성공 단 **dispatch infrastructure (bench harness `.hexa` · run.sh · parser_template) 가 22 중 0/22 fully ready** → g63 honest halt. **build phase 와 fire phase 의 경계가 spec 에 없어** 사용자가 잘못된 tool 선택 (=  /micro-exp before build) 으로 갈 수 있고, fabrication-or-silent-skip 두 가지 failure mode 위험. **제안 5개**: **(1)** `plugins/micro-exp/SKILL.md` Stage 1.5 신설 — per-candidate infra existence check (`inputs_dir` 경로 resolves · `run.sh` OR canonical run pattern · `parser_template` registered) → 미달 시 자동 halt + `🛑 N/M candidates missing dispatch infrastructure — /cycle-bg <active-domain> required first` 한 줄. **(2)** `hooks/commons/commons.tape @D g7N` (sign-gated) — `/cycle-bg = build+fire 통합 (각 worktree agent 가 dispatch infra 작성하고 실행)` · `/micro-exp = fire-only (pre-built infra 가정)` · candidate matrix 있고 infra 미작성 시 `/cycle-bg` default · "spec 명시 없이 도메인 doc 에 'default = /micro-exp' 라고 적기" 금지. **(3)** `local-pool adapter` 확장 — LLM bench wall measurement (`cx_lab_sandbox` — ECE · NIAH · TruthfulQA · IFEval · IFEval format compliance · gCO2/token · ...) 명시 (현 spec 은 DFT-elph/SSCHA/materials wall-measurement kind 가정) · substrate mac M3·ubu-1·ubu-2 · SKIP `/atlas register` (LLM bench wall ≠ atlas atom, persist verbatim to `.verdicts/`). **(4)** meta-domain (candidate orchestrator) 패턴 docs — 신규 `docs/meta-domain-pattern.md` 또는 `plugins/domain/SKILL.md` 보강. sibling-of-siblings · @goal "cross-domain orchestrate" · milestone 형식 `- [ ] <CANDIDATE>/A<n> — <probe> · 반증자: <falsifier>` · "운영 전략 (multiple dispatch strategies)" 섹션 권장 (⭐ PRIMARY 1 + 보조 N · milestone 별 권장 strategy 매핑). honesty invariant: "default strategy" 는 infra 완성 후의 default — build 미완 시 자동으로 build-fire 통합 strategy 로 fall back. Reference 예시: hexa-codex `CODEX/CODEX.md` (PR #126 · 22 candidate orchestrator). **(5)** `/cycle-bg` ↔ `/micro-exp` 결정 트리 cross-link in 양쪽 SKILL.md: `candidate matrix in context? → NO=/kick · YES → dispatch infra 존재? → NO=/cycle-bg · PARTIAL=/cycle-bg then /micro-exp · YES=/micro-exp`. 영향: `plugins/micro-exp/SKILL.md` (Stage 1.5 + local-pool LLM 확장 + cross-link · 0.X.0 bump) · `plugins/cycle/SKILL.md` (cross-link) · 신규 `docs/meta-domain-pattern.md` · `commons.tape` @D g7N (sign-gated) · README/CHANGELOG. 인접 RFC: `no-fake-closure-verdict-gate.md` (verdict-gate 강화 — 같은 honest-sweep 계열).

- [x] **easy-auto '설명'/'쉽게' 트리거 + banner** ✅ (from user 2026-05-28) — easy-auto 0.2.0: 한국어 substring 매칭 추가 (verb/noun/adverb 어형 catch · 오탐 수용) + 발동 시 7-요소 banner. 상세 → `INBOX.log.md`.

- [x] **sbs 0.5.0 재설계** ✅ (from user 2026-05-28) — 2 modes (manual=chat-form default·auto=4축 가중평균 자동선택·inline override) + plan.md 자동생성 + 'go' handoff. 상세 → `INBOX.log.md`.

- [x] **pool-route path-whitelist + sidecar paths CLI** ✅ (from user 2026-05-28 — chat-form sbs FULL 4축 합의) — pool-route 0.10.0 + sidecar 0.6.0: `~/.sidecar/local-paths` (line-based plain text · USER 가 `sidecar paths {bare|add|rm}` 로 관리) — argv 의 `/`-prefix 토큰이 등록 prefix 로 시작하면 LOCAL 강제 (sign-local 의 영구 카운터파트 · TTL 없음). 첫 호출 시드 = `~/.claude/plugins/cache/sidecar/` + `~/core/sidecar/` (sidecar 자기 hexa 호출이 더이상 pool 로 안 튕김). 화이트리스트 자체가 opt-in (s11). 상세 → `INBOX.log.md`.

- [x] **stale-base squash-merge 35190-삭제 방어** ✅ — pr-cycle deletion-sanity-gate (0.2.0 · 실질 deny) + worktree-guard 브랜치재사용 advisory (0.2.0) + git-guard stale-base 머지경로 advisory (0.6.0) (from anima #1105 2026-05-27). 35190 D/3 A 이상치 smoke DENY 확인 · 정상 PR pass. 상세 → `INBOX.log.md`.

- [x] **`pool` CLI 회귀 — 모든 subcommand 미실행, `OK: <첫-인자>` stub 만 출력** ✅ 로컬 해소 (from hexa-lang RUNTIME 2026-05-26) — **근본원인**: shim 의 implicit-run(`hexa <file>`)이 Darwin 에서 build 출력을 `/tmp` 로 잡아 → 커널패닉 가드가 build **REFUSE** → 실패 경로가 `bin/pool.hexa` 소스를 259B C stub 로 **clobber**(매 호출마다 소스 자가파괴). m3b 툴체인 교체(`~/.hx/bin/hexa.real`→hxv2 컴파일러, 05-26 05:47)가 AOT 캐시를 무효화 → 강제 recompile → 이 게이트에 매번 충돌. (shim·HEXA resolution 은 정상이었음 — 핸드오프 1차추정 정정.) **로컬 fix**: pool 을 네이티브 바이너리로 선컴파일(`HEXA_MAC_BUILD_OK=1 HEXA_LANG=… hexa-run build … -o ~/.hx/bin/pool.bin`) 후 `~/.hx/bin/pool` shim 을 그 바이너리 직접 exec 로 repoint(원본 shim → `pool.shim-bak-2026-05-26`). `pool list`/`status`/`on ubu-2` 전부 정상, 소스 무손상, g9 offload 복구. **남은 상류 debt**(sibling repo): (1) hexa-lang run-driver — Mac 기본 build 출력 `/tmp` 회피 + **build 실패 시 소스 clobber 금지**, (2) `dancinlab/pool` install.hexa — 첫 설치에 네이티브 선컴파일(현 shim 의 implicit-run 의존 제거). 상세 → `INBOX.log.md`.

- [x] **`/micro-exp` (context-driven sweep) + commons.tape g63-g66** ✅ (from demiurge RTSC 2026-05-25) — skill 0.2.0 **context-driven 재설계** land (manifest 파일 없이 `/cycle`처럼 맥락에서 candidate self-enumerate · 바=현재 맥락·인자=스코프) + commons.tape **g63 honest-sweep · g64 budget-cap · g65 aggregation · g66 pod≠agent-cap** land (유저 `sidecar sign commons` 후). 상세 → `INBOX.log.md`.
- [x] **worktree/branch 하네스 4-gap** (from anima 2026-05-25) — sidecar 몫 **#4(pr-cycle `gh pr merge` cross-repo 오작동) ✅ 해소** (pr-cycle 0.3.6 — `--repo` 파싱→merge 전파 · cross-repo 시 로컬 worktree cleanup skip). 남은 **#1**(공유 .git HEAD main 이동)·**#2**(로컬 브랜치 ref 유실)·**#3**(worktree agent PR 무관파일 bundle)은 **harness-upstream**(Anthropic `isolation:worktree` 영역) — sidecar 직접 fix 불가, 추적만. 상세 → `INBOX.log.md`.
- [x] **worktree-gc 가 활성 사용 중 worktree 를 prune (브랜치명 재사용 → "origin-gone" 오판)** ✅ 해소 (from demiurge monograph fan-out 2026-05-26) — **worktree-gc 0.2.0 — dirty/recent-mtime/cwd-in-use/HEAD-ancestor 가드 + atomic prune** land. 근본원인: prune 판정이 브랜치명 기준(`origin/<branch>` ref 소실 → "origin-gone" → 파괴)이라, 이전 세션이 동명 브랜치(`feat/cern-monograph`)를 merge→삭제하면 동명 재사용한 신규 worktree(미푸시 라이브 작업)가 오판 파괴됐다. fix: ref 소실 신호 폐기 → **HEAD 가 실제 `origin/main` ancestor 인지**(`git merge-base --is-ancestor`)로만 prune 판정 + 4중 라이브 가드(dirty·mtime<1h·cwd-in-use·HEAD-ancestor) + `git worktree remove` 단일 삭제경로(실패 시 무삭제=비원자 상태 불가). `hexa parse` clean + 가드 primitive smoke test ✅. CERN(#220)·ANTIMATTER(#222) 재발 차단. 상세 → `INBOX.log.md`.
- [x] **pool.json roster race-wipe 방어** ✅ (from demiurge RTSC 2026-05-25) — **pool 0.8.5 에서 해소** (sibling repo `dancinlab/pool` · `601a42d`). `_save` 비원자 truncate-rewrite 가 원인이었고 세 겹 방어로 land: **원자 write**(temp→`mv -f` rename(2)) + **empty-clobber 가드**(살아있는 roster 를 빈 것으로 덮으면 거부 · `pool rm` 만 예외) + **.bak 로테이션**(+ `_load` 빈/torn 파일 시 `.bak` 복구). 30-writer 동시 storm 무손상 검증 · `hx install pool` 로 로컬 라이브. 제안 5종 중 **flock 직렬화는 deferred**(macOS `flock(1)` 부재 · 원자 write 가 관측된 wipe 를 이미 제거 · lost-update 는 저severity). 상세 → `INBOX.log.md`.
- [x] **`/domain` 아이콘+별칭 타이틀/서브타이틀** ✅ (from anima IIT4 2026-05-25) — **domain 0.8.5 에서 land**. 옵션 `@title:` 필드 + `/domain title <text>` (alias `subtitle`) 서브커맨드 + bare `/domain`·`set` 출력 렌더(`@title:` 있으면 `◆ 🧠 IIT4 — "의식 측정자(尺)"   🎯 <goal>`, 없으면 현행 plain). non-breaking(미설정=현행 동작) · lint 은 `@title:` 부재 무경고(취향 필드). easy plugin 7요소와 도메인 트래커 정합 갭 해소. 상세 → `INBOX.log.md`.
- [x] **pool data-locality + sign-local 토큰 < build 시간** ✅ (from anima PURE 2026-05-25) — **pool-route 0.9.3 에서 양쪽 해소**: (a) 데이터-locality pin — hexa exec 이 데이터-입력 플래그(`--corpus-path`·`--data`·`--weights`·`--ckpt` 등 11종)를 들고 그 값이 동기화 `~/core/` workdir 이 아닌 로컬-only 경로면 local pin(피어엔 데이터 없음 → 라우팅 무의미). (b) sign-local TTL `5min→30min`(`LOCAL_SIGN_TTL=1800`)으로 10-20분 build 커버 — 거버넌스 sign TTL(300, commons/project 편집 게이트)과 독립. smoke 12+3 ALL PASS. 상세 → `INBOX.log.md`.
- [x] **`/sbs` FULL mode UX — 채팅 폼 + 합의 화면** ✅ (from demiurge 2026-05-27) — step-by-step 0.4.0: FULL 자체가 chat-form (1Q/round + easy-mode 7-요소 scaffold + 자유응답) · 라운드 종료 후 `🎯 합의된 결정셋` ASCII tree pause (user `go` / `Qn=<other>` 수정) · mid-run new ambiguity도 chat + 재합의. AskUserQuestion 4-옵션 selectbox 강제 해소. 상세 → `INBOX.log.md`.

- [x] **paper plugin v0.8 3-verb 버그** ✅ (from demiurge ANTIMATTER paper rego PR #197) — paper 0.11.0: `pr-roll` bare repo 친절 reject + jq `\\#` escape (LaTeX `\#` 살리기) · `atoms` `LC_ALL=C` + lowercase compare (BSD awk) + `fn <name>(` dispatch 매칭 · `arxiv-prep` absolute tar path (`$TMP/<dir>` 트랩 해소). 상세 → `INBOX.log.md`.

- [x] **활성 POD/job 작업 매니페스트** ✅ (from demiurge RTSC 2026-05-27) — `hexa cloud pods` + `hexa cloud dispatch {tree|active|add|verdict|rm}` (PR #1699 main land) · cwd `./pods.json` per-project (update-form) · sidecar `/cloud` 0.4.0이 NL 트리거 + description 노출. INBOX 원안의 `~/.pool/pods.json` 글로벌 + pool CLI는 사용자 정정으로 cwd + hexa cloud로 재라우팅 (pool=호스트 roster · pods=작업 매니페스트 영역 분리). 글로벌 `pod_registry` (`cloud orphans` · billing/orphan 자동추적)와 분리. 상세 → `INBOX.log.md`.

- [x] **g61 stdlib-SSOT 강제·자동화·범위확장** ✅ (from anima IIT4 2026-05-25) — 4/4 모두 land: (1) **g61 범위확장** commons 0.10.7 (primitives→+domain engines · engine⊥adapter · import-root) `30620ee`, (2) **stdlib-ssot-guard 0.1.0** hook (PreToolUse 비차단 advisory: anima-locked import + stdlib dup-fn · SessionStart stdlib-root 검증, guard 4-test PASS), (3) **stdlib 0.1.0** skill (`/stdlib check`·`promote` 런북), (4) import-root = SessionStart 체크. user `sidecar sign commons` 후 g61 land. 선행 사례: anima IIT4 → stdlib/consciousness/iit4 (hexa-lang #1051) + anima shim (#542) + hexa-brain EEG adapter (#1). 상세 → `INBOX.log.md`.
