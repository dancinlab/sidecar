# POOL-OFFLOAD — log

Append-only history sister of `POOL-OFFLOAD.md`. Each entry starts with `## <ISO timestamp> — <header>` (newest on top); body = `- [x]` (done) / `- [ ]` (pending) checkbox tasks.

