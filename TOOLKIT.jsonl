{"id":"install","kind":"setup","usage":"install [--no-hooks] [--ref main] [--dry-run]","use":"COMMON/global setup — clone dancinlab/sidecar → ~/.sidecar/cli + a sidecar wrapper on ~/.local/bin + global hooks (idempotent). NOT a per-repo scaffold (that's init). curl one-liner: curl -fsSL https://raw.githubusercontent.com/dancinlab/sidecar/main/scripts/install.sh | bash"}
{"id":"init","kind":"setup","usage":"init [--force] [--dry-run]","use":"scaffold THIS repo: config + .harness rules + gitignore + wrapper (hooks are GLOBAL-ONLY → sidecar install) (strict by default: block-everything + branch protection + pre-push verify + single-doc scaffolds)"}
{"id":"lab","kind":"setup","usage":"lab init [dir] [--force] [--dry-run] [--name N] [--desc D]","use":"scaffold the lumen/rtsc/carbon-capture sibling-repo \"lab\" skeleton into a target repo: src/ · state/ · ARCHITECTURE.json + architecture.html viewer + serve.py · CLAUDE/README/CHANGELOG · HYPOTHESES/ (pre-register→falsify→run→verdict) · tool/<slug>.py shared harness (tokenized templates/lab/ · never-clobber-unless --force)"}
{"id":"uninstall","kind":"setup","usage":"uninstall [--dry-run] [--keep-logs]","use":"remove sidecar-injected files (config/.harness/hooks/wrapper); keeps user content"}
{"id":"update","kind":"setup","usage":"update [--hooks]","use":"bump .harness-engine submodule to latest (adopt new engine features) + optional hook refresh"}
{"id":"install-hooks","kind":"setup","usage":"install-hooks [--global]","use":"merge sidecar hooks into the GLOBAL ~/.claude/settings.json (per-repo --repo is banned → double-inject)"}
{"id":"pi","kind":"setup","usage":"pi {install|status|remove}","use":"wire sidecar into the Pi coding agent — symlink the bridge extension (pi/sidecar.ts) into ~/.pi/agent/extensions/ + add ~/.claude/skills to Pi settings. Same engine as the CC plugin; governance parity (Stop-gates CC-only)"}
{"id":"self-update","kind":"setup","usage":"self-update","use":"git-pull the sidecar CLI clone this binary runs from (e.g. ~/.sidecar/cli) to latest main"}
{"id":"shadow","kind":"setup","usage":"shadow [plan|remove|--force]","use":"mirror sidecar's own commands/ into ~/.claude/commands/ as bare /cmd delegators (marker-tracked · regenerable · --force heals pre-marker stale shadows from source)"}
{"id":"ship","kind":"setup","usage":"ship [--no-doc]","use":"SIDECAR-REPO ONLY — propagate sidecar's OWN change across its install surfaces: inject-bloat guard (context-rot) → pr-cycle → self-update (global CLI) → shadow (slash mirror). Other repos use pr-cycle for a plain verified merge"}
{"id":"pre","kind":"hook","usage":"pre bash","use":"PreToolUse(Bash)  — enforcement match → block/warn"}
{"id":"pre-write","kind":"hook","usage":"pre write","use":"PreToolUse(Write/Edit) — path/content rules"}
{"id":"pre-askq","kind":"hook","usage":"pre askq","use":"PreToolUse(AskUserQuestion) — deny option-box → ask in plain chat (config.askqText)"}
{"id":"pre-tool","kind":"hook","usage":"pre tool","use":"PreToolUse(mcp__.*) — annotation-guard: classify MCP tool vs config registry → warn/block (Rule-of-Two)"}
{"id":"post","kind":"hook","usage":"post bash <exit> [cmd]","use":"PostToolUse(Bash) — record + route non-zero exits"}
{"id":"post-edit","kind":"hook","usage":"post edit <file>","use":"PostToolUse(Write/Edit) — flag L0 edits"}
{"id":"prompt","kind":"hook","usage":"prompt <text>","use":"UserPromptSubmit  — keyword triggers + prompt hints"}
{"id":"commons","kind":"hook","usage":"commons {inject|show}","use":"always-on cross-project governance SSOT (config/commons.md; repo override .harness/commons.md)"}
{"id":"architecture","kind":"hook","usage":"architecture {inject|show|search <q>|lint|convergence ...|result ...}","use":"surface repo-root ARCHITECTURE.json/.md (design SSOT) at SessionStart; search <q> = substring over id/name/role/detail → matching node ids+breadcrumb; lint = c4 tree hygiene + id presence/uniqueness; convergence {list|add|rm|edit} = 재발학습 store CRUD (id-keyed · add upsert · value/threshold '--value -'=stdin); convergence stop-check = Stop 훅 — 에이전트 출력의 재발-신호 키워드 스캔 → SSOT 기록 넛지(신호별 세션당 1회 · advisory · 패턴=config/convergence-triggers.json); gate-stop-check = Stop 훅 — 출력의 게이트/verdict 신호 감지 시 `🔬 GATE` 마커 없으면 block → ARCHITECTURE.json 의 해당 `type:\\\"gate\\\"` 노드 verdict 를 직접 update-in-place 유도(자동 store 아님 · 패턴=config/gate-signals.json); result = DISCARDED (실험 verdict 는 별도 store 아님 → ARCHITECTURE type:\\\"gate\\\" 노드 verdict 직접 update-in-place · gate-stop-check 강제)"}
{"id":"changelog","kind":"hook","usage":"changelog {add \"<title>\"|list [N]|render [N]|prune --keep N|--older-than D|autoprune|migrate}","use":"append-only history as CHANGELOG.jsonl (newest-first JSONL · ts+title+body) — add appends(body via stdin) + auto-trims to keep-N(config lint.changelog.keep, default 30), prune deletes old entries(keep-N or age), autoprune = SessionStart-wired trim to keep-N (silent under cap · history stays in git), render = markdown view, migrate = one-shot CHANGELOG.md→.jsonl. The CHANGELOG-MISSING gate now wants CHANGELOG.jsonl staged"}
{"id":"git-context","kind":"hook","usage":"git-context {inject|show}","use":"SessionStart + per-turn-when-stale: warn when HEAD is BEHIND origin/<default> (stale-branch trap — reading pre-merge code as current). Silent on a clean on-default checkout."}
{"id":"claudemd","kind":"hook","usage":"claudemd {inject|show}","use":"re-inject repo-root CLAUDE.md (project rules) EACH UserPromptSubmit so they stay enforced (optional <!-- enforce:start/end --> block)"}
{"id":"toolkit","kind":"hook","usage":"toolkit {list|inject|json|write|check}","use":"command catalog (SSOT = this HELP) — inject surfaces the WHOLE command surface at SessionStart so an agent knows every cmd; check gates TOOLKIT.jsonl drift"}
{"id":"companions","kind":"hook","usage":"companions {inject|list}","use":"sibling-CLI command surface — inject runs each configured neighbour CLI's catalog (config `companions` + ~/.sidecar/companions.json · DOMAIN-AGNOSTIC, e.g. hexa) at SessionStart so the agent knows `hexa cloud` exists without probing"}
{"id":"prefs","kind":"hook","usage":"prefs {show|code|docs|response <lang>|inject}","use":"language prefs (3 axes) + UserPromptSubmit inject"}
{"id":"easy","kind":"hook","usage":"easy {show|inject|scaffold \"<q>\"|lint <file|->}","use":"easy friendly-response style — inject (lang from prefs) · scaffold = empty 7-element round skeleton · lint = advisory axis score (no LLM)"}
{"id":"fable-mode","kind":"hook","usage":"fable-mode {on|off|status|inject} [--repo]","use":"session-scoped toggle — when ON, per-turn inject splits work: DESIGN/ANALYSIS delegated to Fable 5 via 'sidecar fable', IMPLEMENTATION done locally (scope: repo .harness > host ~/.sidecar; default host-wide · OFF emits nothing)"}
{"id":"load","kind":"hook","usage":"load {show|inject}","use":"per-turn macOS resource readout (CPU load + RAM pressure/used% + swap, ⚠️ on danger) — UserPromptSubmit inject"}
{"id":"recommend","kind":"hook","usage":"recommend {inject|show|get-default|set-default <present|auto|axis|axis+axis…> [--global]|clear-default [--global]|resolve-mode <a>}","use":"4-axis rubric + default mode (repo .harness > global ~/.sidecar > present; fixed axis = auto-pick)"}
{"id":"goal-guard","kind":"hook","usage":"goal-guard stop-check","use":"Stop gate — blocks a reply that PUNTS the work: defers to a future session ('다음 세션에…'/'next session', tail-precise) OR reports live '잔여' (leftover work, negation-aware) — so the agent finishes now; single-fire (stop_hook_active), genuine blocker passes on re-stop (session-terminal)"}
{"id":"sbs","kind":"hook","usage":"sbs [auto[:<axis>]|manual] [<task>]","use":"plan-first runbook — resolver-first mode · chat-form 모호성→0 · plan.md handoff + auto-QA 4축 + 9-section dossier"}
{"id":"abg","kind":"hook","usage":"abg [labels]","use":"all-bg-go — fan out prior-turn branches as parallel background Agents (runbook)"}
{"id":"afg","kind":"hook","usage":"afg [labels]","use":"all-fg-go — run prior-turn branches sequentially in-session (runbook)"}
{"id":"fleet","kind":"hook","usage":"fleet [name:goal,…|go|stop|status]","use":"perpetual multi-lane orchestrator (runbook + roster)"}
{"id":"fleet-lab","kind":"hook","usage":"fleet lab [frontier:wall,…|go|…]","use":"research-driven frontier lab (research-gate→implement→measure→SSOT→re-research; walls measured + reopenable)"}
{"id":"fleet-abstract","kind":"hook","usage":"fleet abstract [layer:seed,…|go|…]","use":"abstraction-driven layer dive (census LAWS→peel to shared trade-off/meta-law→invent escape→cast as falsifiable prediction; meta-laws reopenable · d6 honest)"}
{"id":"fleet-full","kind":"hook","usage":"fleet full [frontier:goal,…|parallel|go|…]","use":"full-stack campaign — ALL 3 phases in order per frontier (research→implement→abstract→falsify · implement NEVER skipped, weak lever still measures a wall before abstract) · SEQUENTIAL by default (afg-style; pass 'parallel' to fan out) · cheap implement auto, only paid gates (c14)"}
{"id":"pr-cycle","kind":"hook","usage":"pr-cycle [--no-reap] [gh flags]","use":"push branch → open PR → self-merge (squash·admin·delete-branch) → reap stale open PRs (--no-reap skips)"}
{"id":"reap","kind":"hook","usage":"reap [--max-refresh N] [--no-close] [--dry-run] [--artifact RE]","use":"drain stale open PRs: merge MERGEABLE (no-admin) · refresh-merge CONFLICTING (doc-files auto-resolved, code conflicts abort) · ≥7d code-conflict PRs closed with branch preserved · cron-able"}
{"id":"pod","kind":"hook","usage":"pod","use":"GPU cloud pod dispatch runbook (preflight→fire→poll→harvest→down · cost-gated)"}
{"id":"pod-poll","kind":"hook","usage":"pod poll <id> [--ssh-check \"<cmd>\"|--done-match RE] [--teardown-on-done] [--pull \"<remote> <local>\"]","use":"one-shot auto-poll via hexa cloud (alive→util/probe→optional pull+teardown · READ-ONLY default · pull-then-destroy)"}
{"id":"dojo","kind":"hook","usage":"dojo [<slug>] [--lang]","use":"cloud training-job scaffolder (runbook + exports/dojo/<slug>/ emit)"}
{"id":"micro-exp","kind":"hook","usage":"micro-exp [<scope>]","use":"context-driven micro-experiment sweep (infra-gate→budget→dispatch→monitor→absorb→ledger)"}
{"id":"bypass","kind":"hook","usage":"bypass","use":"anti-punt self-check runbook (proceed on local+reversible; ask only when outward/decision)"}
{"id":"go","kind":"hook","usage":"go","use":"continue the most-recently proposed action without re-confirming"}
{"id":"brainstorm","kind":"hook","usage":"brainstorm","use":"iterative ideation rounds until depletion (breadth over selection)"}
{"id":"demi","kind":"hook","usage":"demi","use":"design-architecture program runbook (7-verb spine)"}
{"id":"gap","kind":"hook","usage":"gap [full|list|<scope>]","use":"multi-axis gap exploration — 40 breakthrough lenses (8 families) · triage→deepen runbook"}
{"id":"pool","kind":"hook","usage":"pool {list|add|rm|on|status|specs}","use":"host roster + remote exec + cores/mem/GPU probe (~/.sidecar/pool.json, global)","triggers":["호스트 로스터","host roster","원격 실행","원격에서 실행","어느 머신","어느 호스트","남는 머신","비는 머신","유휴 머신","idle host","원격 머신","다른 컴퓨터에서","pool list","pool status","머신 목록","호스트 목록","서버 목록","cpu 여유","gpu 여유","어디서 돌릴","ssh 로 돌"]}
{"id":"mem-guard","kind":"hook","usage":"mem-guard {status|check|install|uninstall}","use":"OOM prevention — free-RAM preflight before bg-spawn + opt-in launchd notify watchdog"}
{"id":"secret","kind":"hook","usage":"secret <verb> [args]","use":"passthrough to the secret CLI (Keychain creds · get/set/rotate/list/init/backup/sync) ⚠ `get` exposes the value in context — prefer inline `$(secret get <k>)` for tool args","triggers":["api key","API 키","api 키","키가 필요","키 필요","credential","자격증명","access token","비밀키","인증 정보","토큰 저장","secret get","secret set","키 저장"]}
{"id":"lsp","kind":"hook","usage":"lsp {wire|status|rebuild <file>}","use":"editor LSP wiring (.lsp.json; hexa-lang `hexa lsp` for .hexa by default) + background rebuild of prebuilt hexa LSP binaries when their grammar source is edited"}
{"id":"research","kind":"hook","usage":"research {arxiv <query|id> [--n N] [--sort relevance|date|updated] | yt <url|id> [lang] | web <query> [--n N] | fetch <url>}","use":"arXiv / YouTube transcript / keyless web search (DuckDuckGo) / page fetch (no key)","triggers":["arxiv","arXiv","논문 찾","논문 검색","선행연구","관련 연구","related work","survey 논문","서베이","SOTA","최신 연구","preprint","literature review","유튜브 자막","youtube transcript","영상 자막","transcript 추출"]}
{"id":"watch","kind":"hook","usage":"watch <url|path> [question] [flags]","use":"download (yt-dlp) → frames (ffmpeg) + transcript (captions/Whisper) for the agent","triggers":["영상 분석","비디오 분석","동영상 분석","영상 요약","프레임 추출","영상에서 추출","유튜브 영상 보","video frames"]}
{"id":"fable","kind":"hook","usage":"fable [flags] <prompt...> | --file <f> | -","use":"delegate ONE instruction to the Fable 5 model via headless 'claude -p' (default -m claude-fable-5 · prompt from argv words/--file/stdin, sent via child stdin — no argv leak/quoting · --json = claude --output-format json · --dry = print resolved argv, no run · --cwd <dir> · --sources <l> = setting sources (default project,local — DROPS the global governance hooks that stall a headless child in a dirty repo; pass user,project,local to inherit the full session env) · -c/--continue = continue the most recent conversation in --cwd · -r/--resume <id> = resume a session_id from a prior --json run (stateful · same --cwd) · --timeout <s> = kill a stalled run after s seconds (default OPT-IN cap, default UNLIMITED, exit 124) · flags after -- go to claude verbatim) · --bg = fire-and-forget (launch detached → prints a job id, returns now); collect with 'sidecar fable result <id>' (RUNNING=exit3) / 'tail <id>' (follow output live) / 'wait <id> [--timeout s]' / 'list' — no hand-rolled poll loop · --write (aliases --bypass/--agent) = grant WRITE/EXECUTE via --permission-mode bypassPermissions (Write/Edit/Bash/git · no prompts) so the child can implement; DEFAULT is READ-ONLY (headless can't approve, so writes auto-deny — Read/Grep/Glob only) · opus fallback FORBIDDEN (always, not a flag): pins an availableModels allowlist without Opus, so a safety-classifier-flagged request refuses on the delegated model instead of silently re-running on Opus)","triggers":["fable","Fable","파블","페이블"]}
{"id":"imagine","kind":"hook","usage":"imagine <prompt-file> <out.png> [-s size] [-b fal|openai] [-m model] | list | help | history","use":"AI image generator (fal/openai · keys via secret · prompt from FILE · canonical sizes)","triggers":["이미지 생성","그림 생성","이미지 만들","그림 그려줘","generate image","image generation","일러스트 생성","썸네일 생성","커버 이미지","dall-e"]}
{"id":"email","kind":"hook","usage":"email send --to <a> --subject <s> [--from <a>] [--text <file>|-m <inline>] [--html <file>] [--cc][--bcc][--reply-to][--tag][--stream][--attach <f>]... [--dry]","use":"transactional email via Postmark API (POST /email) — token from `secret get postmark.server_token` (curl -K · never in argv) | history [--count N][--offset N][--tag t][--json][--local] | list | help"}
{"id":"paper","kind":"hook","usage":"paper <new|build|cover|list|publish|update|unpublish|status> [slug] [flags]","use":"demiurge-house scientific paper: scaffold (emoji title · g5 tier badges · TikZ+pgfplots · fal.ai cover) → xelatex+bibtex×3 build → g51 ≥10-page gate publish --to zenodo|arxiv|both [--sandbox][--source]: Zenodo REST lifecycle (DOI) · arXiv submission tarball+guide (no API) update (Zenodo new-version) · unpublish (delete Zenodo draft) · status — keys via `secret get zenodo.token`","triggers":["논문 작성","논문 빌드","논문 써","scientific paper","페이퍼 작성","논문 스캐폴","latex 논문","논문 초안"]}
{"id":"lint","kind":"gate","usage":"lint [all|fast|verbose]","use":"staged-L0 + freshness + doc-gate checks"}
{"id":"naming","kind":"gate","usage":"naming audit [path] [--ing] [--gate]","use":"repo-wide non-canonical name audit (version/copy/dup suffix backlog the write-guard never saw) · --ing = land summary on THIS repo's board · --gate = exit 1 on any hit"}
{"id":"hypotheses","kind":"gate","usage":"hypotheses {check [--gate]|migrate <dir>|scaffold|show}","use":"enforce ONE canonical hypothesis folder (config hypotheses.dir, default HYPOTHESES/) for pre-register->falsify->run->verdict work. The pre write/bash guards block NEW strays (a configured hypotheses.alias, e.g. anima UNIVERSE, or the built-in hypothes*/가설* pattern); check audits the backlog (--gate exits 1), migrate = history-preserving git mv <dir> -> HYPOTHESES/ (merges if it exists), scaffold = create the skeleton, show = active dir + aliases","triggers":["가설","hypothesis","hypotheses","pre-register","반증","falsif"]}
{"id":"ci","kind":"gate","usage":"ci [all|fast|list|scaffold [--force]]","use":"run configured verification commands in parallel (was verify; config key stays verify.checks) · scaffold = emit a .github/workflows/ci.yml that runs 'sidecar ci' on config ci.{runner,setup,fallback,cachePaths} (init writes it too) · ci.fallback on = cost-free fast path (self-hosted pool then github-hosted fallback, probe-fail-safe, no Blacksmith) + cachePaths = warm actions/cache","triggers":["commit","커밋","push","푸시","ship","배포"]}
{"id":"ci-track","kind":"gate","usage":"ci-track <pr|branch> [--watch] [--interval=60] [--timeout=1800] [--merge-on-green] [-R owner/repo]","use":"track remote PR/CI checks (gh) → 🟢/🔴/🟡 verdict; --watch polls until terminal (no hand-rolled gh-poll loop)"}
{"id":"verify","kind":"gate","usage":"verify [rubric|fence \"<claim>\"]","use":"tier-rubric claim verification (badges · no self-judge)"}
{"id":"errors","kind":"gate","usage":"errors {route|list|drain_check|mark_fixed}","use":""}
{"id":"ledger","kind":"gate","usage":"ledger {register|complete|list|gc|dup_check}","use":""}
{"id":"bitter-gate","kind":"gate","usage":"bitter-gate audit [window]","use":"rule-hit frequency → retire dormant rules"}
{"id":"audit","kind":"report","usage":"audit [full|summary|json]","use":"6-axis self-scorecard"}
{"id":"gc","kind":"report","usage":"gc [scan|drift]","use":"broken markdown links in guides"}
{"id":"injects","kind":"report","usage":"injects","use":"per-turn inject footprint report (per-source bytes/tokens + aggregate vs injectBudgetBytes) — context-rot visibility"}
{"id":"docs","kind":"report","usage":"docs [status|check|scratch [name]]","use":"single-doc discipline (architecture SSOT + log + scratch + quickref) write-time enforced in `pre write` (docs.enforce: warn[default]|block|off)"}
{"id":"lockdown","kind":"report","usage":"lockdown {status|add <path...>|rm <path...>|check <path>}","use":"manage L0 set (opt-in · none until designated) add/rm mutate harness.config.json lockdown.files"}
{"id":"folders","kind":"report","usage":"folders [scan|scaffold <dir>]","use":"per-subfolder CLAUDE.md coverage + scaffolding (enforced: lint blocks FOLDER-GUIDE-MISSING for the folder of any staged file lacking a guide · commons folder-docs)","triggers":["폴더 구조","폴더별","서브폴더","디렉토리 설명","folder guide","module doc","코드 탐색","구조 파악"]}
{"id":"end","kind":"report","usage":"end","use":"session-closure safety check (uncommitted·unpushed·stash·PRs·branches·worktrees)"}
{"id":"worktree","kind":"report","usage":"worktree {scan|gc|inject|guard <cmd>}","use":"no-pileup/no-stranded enforcement — flag stranded worktrees · auto-sweep merged([gone]) + aged(>maxAgeDays, tip→refs/reaped) (`inject`=SessionStart/Compact WARN surfacing stranded worktrees+no-worktree branches+refs/reaped from prior sessions · `scan` exit 1 gates new work · `gc` auto-sweep)"}
{"id":"ing","kind":"report","usage":"ing [show|add|done|next|pod ...|inject]","use":"in-progress board → ING.jsonl (작업·POD·next · done=scrub · SessionStart inject · 내 repo 전용 — cross-repo 전달 폐기)","triggers":["handoff","인계","wrap up","마무리","끝내자","진행중","지금 뭐","작업 현황","in progress","pod 관리","돌고 있는","ING.md"]}
{"id":"verdict","kind":"report","usage":"verdict {record <id> <cmd>|list|show <id>}","use":"verification evidence ledger → .verdicts/ (PASS/FAIL)"}
{"id":"atlas","kind":"report","usage":"atlas {add <id> <claim>|link <id> <vid>|list}","use":"claim registry → ATLAS.md (verified via PASS verdict)"}
{"id":"models","kind":"report","usage":"models {list|show|add|set|gate|feat|verify|prune|rm}","use":"model registry → ARCHITECTURE.json .models[] (id·arch·params·tier·sha256·path·hf + gates 검증충족도·progress·features · byte-invariant splice · verify=sha256 · prune=HF+sha guard)"}
{"id":"dataset","kind":"report","usage":"dataset {list|show|add|set|feat|rm} [--lang ko|en] [--register general|sns]","use":"dataset registry → ARCHITECTURE.json .datasets[] (4-cell lang×register · byte-invariant top-level splice · parallel to models)"}
{"id":"upstream","kind":"report","usage":"upstream {list|fix <name|repo>}","use":"in-session upstream (hexa-lang…) fix runbook (no inbox-only defer)"}
{"id":"sync","kind":"report","usage":"sync {run|diff}","use":"run configured shared-file sync script"}
