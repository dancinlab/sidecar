---
description: wilson-fire-gate — instrument-first decision gate (measure over predict). A *fire* = running one real measurement instead of predicting its outcome. Gates each measure-vs-predict fork and maintains the design.md `Fire-decision N` ledger. `decide` appends an entry; `log` shows the ledger; `on`/`off` toggles principle injection; `path` sets the design.md file; `sample` prints the bundled instrument-first convention.
argument-hint: "[status | on | off | decide \"<picked>\" \"<rationale>\" | log | path <file> | sample]"
allowed-tools: Bash
disable-model-invocation: true
---

!`sh "$CLAUDE_PLUGIN_ROOT/bin/fg.sh" cmd $ARGUMENTS`
