---
description: /fleet-abstract [layer:seed,… | go | stop | status] — abstraction-driven layer dive: census the LAWS, peel one layer to a meta-law, invent an escape principle, cast it as a falsifiable prediction. Abstract/unverified (d6). Subcommand of /fleet. Triggers — "fleet abstract", "추상화 함대", "메타법칙 탐색", "/fleet-abstract".
argument-hint: "[layer:seed,… | go | stop | status]"
allowed-tools: Bash
---

!`command -v sidecar >/dev/null 2>&1 && sidecar fleet abstract $ARGUMENTS || echo "sidecar CLI not found — install dancinlab/sidecar (~/.sidecar/cli + ~/.local/bin/sidecar on PATH)"`
