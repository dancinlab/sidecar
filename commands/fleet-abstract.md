---
description: /fleet-abstract [layer:seed,… | go | stop | status] — abstraction-driven perpetual layer dive — each lane is an accumulated law-set / ceiling empirical search couldn't break; rounds census the LAWS, peel one layer to the shared trade-off (meta-law), invent an escape principle, then cast it as a falsifiable prediction handed to compute/research. Meta-laws are reopenable by a new lens. All output is abstract/unverified (d6) — coordinates, not discovery. (Subcommand of /fleet — `/fleet abstract …` is equivalent.) Triggers — "fleet abstract", "추상화 함대", "추상화 방향", "메타법칙 탐색", "상상으로 진행", "/fleet-abstract".
argument-hint: "[layer:seed,… | go | stop | status]"
allowed-tools: Bash
---

!`command -v harness >/dev/null 2>&1 && harness fleet abstract $ARGUMENTS || echo "harness CLI not found — install dancinlab/harness (~/.harness/cli + ~/.local/bin/harness on PATH)"`
