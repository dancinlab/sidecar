---
description: /fleet-full [frontier:goal,… | go | stop | status] — full-stack perpetual campaign that weaves research + implement + abstraction into one auto-phasing loop — each frontier moves itself: cheap research finds untried levers → justified implement+measure → empirical wall (levers exhausted) → auto-promote to abstraction (peel LAWS to meta-law, invent escape) → cast escape as a falsifiable prediction → descend back to research/implement. Walls (🧱) and meta-laws (🌌) both reopenable; depletion needs BOTH axes dry (c14). (Subcommand of /fleet — `/fleet full …` is equivalent.) Triggers — "fleet full", "풀스택 함대", "전체 캠페인", "리서치+추상 순환", "/fleet-full".
argument-hint: "[frontier:goal,… | go | stop | status]"
allowed-tools: Bash
---

!`command -v harness >/dev/null 2>&1 && harness fleet full $ARGUMENTS || echo "harness CLI not found — install dancinlab/harness (~/.harness/cli + ~/.local/bin/harness on PATH)"`
