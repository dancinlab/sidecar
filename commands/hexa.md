---
description: /hexa <subcommand> [args] — passthrough to the `hexa` CLI (native-compiled atlas-aware language toolchain — prover · linker · strict-lint · science stack). Any subcommand runs via `/hexa <sub> …` (cloud, dojo, deck, verify, atlas, …). Triggers — "hexa 실행", "hexa 돌려", "run hexa", "/hexa".
argument-hint: "<subcommand> [args]"
allowed-tools: Bash
---

!`command -v hexa >/dev/null 2>&1 && hexa $ARGUMENTS || echo "hexa CLI not found — install dancinlab/hexa-lang (~/.hx/bin/hexa on PATH)"`
