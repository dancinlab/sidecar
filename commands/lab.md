---
description: /lab init [dir] [--force] [--dry-run] [--name N] [--desc D] — scaffold the lumen/rtsc/carbon-capture sibling-repo "lab" skeleton (ARCHITECTURE.json + viewer · HYPOTHESES/ pre-register→falsify→run→verdict · tool/<slug>.py harness) into a repo. Triggers — "랩 스캐폴드", "lab init", "가설검증 스캐폴드", "scaffold a lab", "/lab".
argument-hint: "init [dir] [--force] [--dry-run] [--name N] [--desc D]"
allowed-tools: Bash
---

!`command -v sidecar >/dev/null 2>&1 && sidecar lab $ARGUMENTS || echo "sidecar CLI not found — install dancinlab/sidecar (~/.sidecar/cli + ~/.local/bin/sidecar on PATH)"`
