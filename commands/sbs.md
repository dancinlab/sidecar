---
description: /sbs [auto[:<axis>]|manual] [<task>] — plan-first sequential runbook (the single-threaded counterpart to /cycle·abg fan-out). Step 1 is resolver-first — sidecar runs `recommend resolve-mode` in CODE and prints the authoritative `mode:`/`resolved:` lines (directive fallback when unsynced). TWO modes share one chat-form scaffold: MANUAL (default · 1 question/round · easy 7-element scaffold · free-form OK) waits per round; AUTO auto-picks each round by 4-axis weighted average (완성도·단순·안전·표준 · default 1:1:1:1 · `auto:safe`/`auto:complete=2,simple=3` override · bare `auto` inherits a FIXED recommend-default axis). Re-scan after every round is a non-skippable hard gate until ambiguity→0, then a 🎯 합의된 결정셋 ASCII agreement screen, then write `drafts/<slug>-plan.md` (frontmatter status:active · @L locked-decision asserts grep/file/verdict · next-action checklist · completion criteria) and HAND OFF to a background Agent on `go` — it ships end-to-end + auto-QA 4축 (functional·visible·conformance·regression · regression FAIL→auto-revert · others→alert) + status flip + 9-section 인계 dossier into the plan's `## handoff` (intra-repo only · NO ad-hoc HANDOFF.md · cross-repo forwarding retired — fix upstream in-session per upstream-fix) + end-user dossier when the plan has a handoff verb. Triggers — "단계별로", "step by step", "계획 먼저", "plan first", "sbs", "차근차근", "/sbs".
argument-hint: "[auto[:<axis>]|manual] [<task>]"
allowed-tools: Bash
---

!`command -v sidecar >/dev/null 2>&1 && sidecar sbs $ARGUMENTS || echo "sidecar CLI not found — install dancinlab/sidecar (~/.sidecar/cli + ~/.local/bin/sidecar on PATH)"`
