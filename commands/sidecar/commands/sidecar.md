---
description: /sidecar <args> — runs `sidecar "$@"` (sidecar marketplace CLI). Verbs — init · sync · mirror · shadow [plan] · unshadow · sign · profile · enable · disable · reset · paths · handoff. `handoff` is the cross-project handoff registry (~/.sidecar/handoff/handoff.jsonl — single SSOT for cross-repo gap/patch/bug/RFC handoffs, retires the per-repo INBOX domain): `handoff add <repo> <text...>` appends an open JSONL entry · `handoff ls [<repo>]` tables entries (filter by repo) · `handoff done <id>` flips status → done · bare `handoff` is a counts summary (JSONL not markdown, store repo-external + portable via $HOME). `shadow` drops the forced `plugin:command` namespace (#15882) — mirror+disable every command/skill plugin (hook/mcp kept), splicing SKILL.md triggers into the bare command so it stays lossless; `shadow plan` dry-runs, `unshadow` reverts. `sign <key>` mints a 5-min user sign-off token that unlocks agent edits to a sign-gated governance SSOT file (commons.tape · project.tape); bare `sign` / `sign clear [<key>]` list / clear tokens. `profile [minimal|hexa|full]` shows/sets the multi-user enable profile; `enable`/`disable`/`reset <plugin>` are per-plugin overrides. `paths` (0.6.0) manages the LOCAL-EXECUTION WHITELIST `~/.sidecar/local-paths`; pool-route 0.10.0 forces any Bash cmd whose argv contains a `/`-prefixed token starting with a registered prefix to run LOCAL — sub-forms `paths` (list) · `paths add` (register cwd) · `paths add <dir>` (register abs-resolved dir) · `paths rm <prefix>`. Pass args through unchanged.
argument-hint: "<init | sync | mirror | shadow [plan] | unshadow | sign <key> | profile [name] | enable <plugin> | disable <plugin> | reset <plugin> | paths [add|rm] [<arg>] | handoff [add <repo> <text...> | ls [<repo>] | done <id>]>"
allowed-tools: Bash
---

!`sidecar $ARGUMENTS`
