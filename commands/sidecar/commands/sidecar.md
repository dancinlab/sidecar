---
description: /sidecar <args> — runs `sidecar "$@"` (sidecar marketplace CLI). Verbs — init · sync · sign. `sign <key>` mints a 5-min user sign-off token that unlocks agent edits to a sign-gated governance SSOT file (commons.tape · project.tape); bare `sign` / `sign clear [<key>]` list / clear tokens. Pass args through unchanged.
argument-hint: "<init | sync | sign <key> | sign | sign clear [<key>]>"
allowed-tools: Bash
---

!`sidecar $ARGUMENTS`
