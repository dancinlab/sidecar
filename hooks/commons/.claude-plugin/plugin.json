{
  "name": "commons",
  "description": "UserPromptSubmit + SessionStart + PreCompact + PostCompact hook — injects a cross-project do/dont layer from commons.tape + a live `pool` roster snapshot (~/.pool/pool.json); UserPromptSubmit re-injects every turn so the layer never fades from context.",
  "version": "0.10.2",
  "author": { "name": "dancinlab" },
  "repository": "https://github.com/dancinlab/sidecar",
  "license": "MIT",
  "keywords": ["claude-code", "hook", "session-start", "shared"]
}
