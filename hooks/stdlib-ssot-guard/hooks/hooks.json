{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'H=\"$HEXA\"; [ -x \"$H\" ] || H=\"$HOME/.hx/bin/hexa\"; [ -x \"$H\" ] || H=$(command -v hexa) || exit 0; exec \"$H\" run \"$@\"' _ ${CLAUDE_PLUGIN_ROOT}/bin/_stdlib_ssot_guard.hexa"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'H=\"$HEXA\"; [ -x \"$H\" ] || H=\"$HOME/.hx/bin/hexa\"; [ -x \"$H\" ] || H=$(command -v hexa) || exit 0; exec \"$H\" run \"$@\"' _ ${CLAUDE_PLUGIN_ROOT}/bin/_stdlib_ssot_session.hexa"
          }
        ]
      }
    ]
  }
}
