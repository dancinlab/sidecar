{
  "name": "walkie-mcp-server",
  "version": "0.1.1",
  "private": true,
  "description": "walkie phase-3 idle-push broker — HTTP/SSE MCP channels server. Tails ~/.sidecar/walkie/<self>.inbox.log and emits notifications/claude/channel per new inbound line. node_modules is NEVER committed; installed on-demand by the SessionStart launcher.",
  "type": "module",
  "main": "walkie-mcp-server.mjs",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
