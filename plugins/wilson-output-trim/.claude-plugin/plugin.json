{
  "name": "wilson-output-trim",
  "description": "Token-economy: rewrites Bash commands (PreToolUse updatedInput) so stdout passes through a TF-IDF line-salience + MinHash near-dup filter before the model ingests it. Spirit-port of wilson's compaction-prefilter, applied at the only lever Claude Code exposes (CC compaction is internal; PostToolUse cannot mutate output).",
  "version": "0.1.0",
  "author": { "name": "dancinlab" },
  "repository": "https://github.com/dancinlab/sidecar",
  "license": "MIT",
  "keywords": ["claude-code", "hooks", "token-economy", "tf-idf", "minhash", "wilson"],
  "hooks": "./hooks/hooks.json"
}
