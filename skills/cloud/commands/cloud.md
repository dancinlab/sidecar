---
description: /cloud <args> — runs `hexa cloud "$@"` (canonical subcommand form per commons g8 · runpod dispatch with structured argv). Subverbs: preflight · run · nohup · poll · tail · copy-to · copy-from. preflight runs a closed-form GPU mem-budget check (no LLM, no pod spinup). tail live-streams a remote log to stdout (Monitor bridge, commons g57).
argument-hint: "<preflight | run | nohup | poll | tail | copy-to | copy-from> [args...]"
allowed-tools: Bash
---

!`hexa cloud $ARGUMENTS`
