---
name: cycle
description: Autonomous work-loop driver — enumerate the active domain's open milestones (commons @D g58) → plan table → fan out one background Agent per item → auto-continue to DEPLETION. The whole family (bare /cycle · /cycle-full · /cycle-loop · /cycle-full-loop · /cycle-all) drains the domain's `## deferred` backlog to the bottom by self-continuing (ScheduleWakeup) each round until open milestones = 0 AND deferred empty AND no other signal. /cycle-all is the "run everything" variant — no per-round cap + no recommend/select gate, fan out EVERY PROCEED item this round (resource-serialized). Triggers — "/cycle", "/cycle-all", "사이클", "계속 진행", "다음 라운드 진행", "keep cycling", "march on", "next round", "전부 진행", "추천 말고 전부". Distinct from all-bg-go (reactive); /cycle self-generates each round, scoped to the session's active domain.
allowed-tools: Agent, Bash, Read
---

@D cycle := "autonomous loop — active-domain next-list → plan → fan-out → auto-continue to depletion" :: skill
  do   = "enumerate active <NAME>.md `- [ ]` milestones (g58) · when next-list empty auto-seed ≤N (default 3) — PRIMARY signal = the domain's own `## deferred` section (promote next batch into the milestone board + drain from deferred), then user hint · prior /gap shortlist · log tail · print plan table · fan 1 bg Agent per item, one msg · auto-continue: bare `/cycle` (and `/cycle-full`) self-continue via ScheduleWakeup each round, draining the whole `deferred` backlog to DEPLETION (cap throttles per-round WIDTH, auto-continue provides DEPTH) — terminate (omit ScheduleWakeup) ONLY when open milestones = 0 AND deferred empty AND no other signal"
  dont = "off-domain enumerate · fabricate seeds with no signal (stop with steer-options when zero milestones + empty deferred + no signal) · poll/sleep · fan destructive ops · nest /cycle · serialize disjoint items · re-seed an already-promoted deferred item (drain it from deferred when promoting) · stop after one round while backlog remains (bare `/cycle` is depletion-driving by default — only stop at true depletion) · schedule a wake-up once all three depletion conditions hold"

@D dup_race_precheck := "auto-skip already-resolved INBOX handoffs AND stale-unflipped domain milestones before fan-out" :: skill [required active]
  do   = "between next-list and plan, run TWO sweeps. (A) INBOX-handoff sweep: for each item naming an INBOX handoff (slug/header present in INBOX.log.md), grep its entry status + `gh pr list --search <slug>` + `git log --all --grep=<slug>` · mark SKIP if any signal resolved-class (fixed · resolved · closed · landed · shipped · absorbed · superseded · merged · `- [x]`). (B) **Stale-milestone sweep (@D stale_milestone_scan)**: for each enumerated open `- [ ]` item, attempt to extract a stable label from the bolded portion (falsifier id like `F-FUSION-*`, RFC id like `RFC-072`, slug-shaped tokens) — if a label exists, run `gh pr list --state merged --search '<label>' --limit 3` + `git log --all --oneline --grep='<label>' -n 3` and test against the same resolved-class regex. If ANY signal fires, mark `SKIP STALE — resolved by <PR#>/<sha>` and SURFACE BEFORE the plan table with a one-line suggestion `→ flip [ ] → [x] in <domain>.md at line <N>`. Items with no extractable label bypass scan-B (PROCEED). Render judgement column in plan table; only PROCEED rows get an Agent."
  dont = "fan out an INBOX handoff without precheck · suppress SKIP silently (always print reason) · grep without label anchor (fp on partial names) · auto-flip the milestone checkbox without surfacing to the user (write-on-detect is destructive; suggest, don't act) · block items naming no INBOX entry AND no extractable label (precheck is opt-in by label presence)"

@D throttle_resilience := "fan-out agents survive rate-limit death — checkpoint-commit + resume contract" :: skill [required active]
  do   = "auto-inject into EVERY spawned Agent prompt at Stage 4: (1) `CHECKPOINT-COMMIT after each milestone (kernel/oracle/measurement) — git add -A && git commit -m 'wip: <milestone>' in your isolated worktree so a rate-limit death loses no work` (2) `if 'Server is temporarily limiting requests / Rate limited' fires, wait briefly and resume — the task remains valid; cite the last checkpoint commit`. Parent-recovery: if an Agent dies mid-flight on throttle, re-fire it as a single-agent retry against the same worktree branch (the checkpoint history is replay-safe — pattern: `feedback-crash-recovery-artifact-pattern`)"
  dont = "spawn an Agent without the checkpoint-commit clause · let the agent treat rate-limit as a fatal · re-fire on a fresh branch (loses checkpoint chain) · checkpoint outside the agent's isolated worktree (cross-agent index race)"

@D worktree_leak_cleanup := "pre-fan-out sweep — auto-remove stale /tmp worktrees + their orphaned local branches left by dead/throttled agents" :: skill [required active]
  do   = "between Stage 3 (plan) and Stage 4 (fan-out), run a safe leak-sweep on `/tmp/wt-*` linked worktrees: for each, if (a) its branch is fully merged into `origin/main` OR (b) its HEAD is older than 1 hour AND has no commits in the last hour AND its branch name matches the active domain's prefix or a fan-out naming convention (`gpu-*`, `cycle-*`, `<domain>-*`) — `git worktree remove <path>` (no `--force` first; if uncommitted changes block, log + skip) then `git branch -D <branch>` (only after worktree removal succeeded). Print one line per cleanup: `cleanup /tmp/wt-<name>: branch <X> merged into main → removed worktree + deleted branch`. Default off-by-flag is FALSE — sweep runs every cycle invocation. Evidence: hexa-lang campaign had to manually `git worktree remove /tmp/wt-<name>` + `git branch -D <name>` before every fresh fan-out after throttle deaths."
  dont = "act on worktree paths NOT under `/tmp/wt-*` (user-managed worktrees in `~/core/<repo>-*` are off-limits) · use `--force` on worktree remove (mask uncommitted user work) · delete a branch whose worktree removal FAILED (orphans the file state) · sweep branches whose name doesn't match a known fan-out convention (false positive on user branches) · sweep on a SHARED working tree's branch (only sweep linked worktrees, never the main checkout)"

@D principle_injection := "Stage 4 auto-injects active cross-cutting feedback memories into every fan-out Agent prompt as governance constraints" :: skill [required active]
  do   = "before issuing each Agent, scan the active project's MEMORY.md for **cross-cutting feedback memories** — markers: (a) hook line in MEMORY.md contains a cross-cutting token (`ALL 도메인` · `모든 도메인` · `cross-cutting` · `cross-domain`); (b) memory file frontmatter has `cross_cutting: true`; (c) memory body contains `**Cross-domain principle**:` or `**모든 도메인**:`. Pick UP TO 3 matched memories (highest signal first by reverse MEMORY.md order = most recent at top), extract each memory's `description` frontmatter + the first `**...**:` paragraph (≤300 chars per memory). Inject as a `Cross-cutting principles (active feedback memories — apply as governance constraints, cite verdicts through this lens)` block in the spawned Agent prompt body, BEFORE the task-specific steps. Evidence: hexa-lang round-7 (PR #1080) framed verdict as raw 'ratio vs cuBLAS' (5.3-6.9×) without the roofline-% lens recorded in `feedback-closure-is-physical-limit` — agent didn't see the principle because it wasn't injected."
  dont = "inject more than 3 principles (prompt bloat, dilution) · inject memories without a cross-cutting marker (random pollution) · let injection block grow >1KB total (cap per-principle ≤300 chars) · forget the cite-verdicts-through-this-lens contract (the principle must shape the agent's verdict framing, not just sit in the prompt) · inject in agents whose task is explicitly off-domain (e.g. a pure infra fix) — the marker can still gate by domain match"

@D resource_contention := "exclusive-resource items serialize, not parallelize — Stage-3 plan table declares per-item resource; Stage-4 issues serial Agents for shared exclusive resources" :: skill [required active]
  do   = "Stage 3 plan-table gains a `resource` column declaring the exclusive resource each item touches — values: `GPU:<host>-timed` (exclusive — only ONE timed agent per host at a time, parallel timed fires poison cuEvent walls), `GPU:<host>-any` (non-exclusive — ptxas/codegen/scp don't contend), `CPU-only`, `Network`, `Filesystem:<repo>`. Stage 4 partitions PROCEED rows by resource value: items with DISTINCT or NON-EXCLUSIVE resource values fan out CONCURRENTLY (one msg, parallel Agents); items SHARING an EXCLUSIVE resource value (e.g. `GPU:ubu-2-timed`) issue SEQUENTIALLY (one Agent at a time, wait for completion before next). Default if not declared: `CPU-only` (non-exclusive). Evidence pattern: hexa-lang GPU campaign round-5 had 4 candidates but only 1 timed-fire (manual serialization on ubu-2)."
  dont = "fan out two `GPU:<same-host>-timed` items in one message (parallel timing on one GPU poisons walls — round-3 attention vs round-4 multi-CTA contention) · over-serialize non-exclusive resources (ptxas compile on the same host while another times — ptxas is brief, doesn't poison) · skip the resource column when GPU/network items are present (silent contention) · re-declare a resource that another agent already owns this round (race)"

@D depletion_not_terminal := "$0-runnable lane exhausted is a PAUSE, not '100% done' — exploratory/perf/limit-bounded domains have an open physical-math-limit frontier (per feedback-closure-is-physical-limit)" :: skill [required active]
  do   = "when the Stage-5 depletion test fires (open milestones 0 · deferred empty · no $0 seed signal), frame the stop as a LANE-PAUSE + open-frontier, NOT terminal completion. 'No cheap runnable next task' ≠ 'domain complete'. Surface the remaining paths that live ABOVE the $0 lane: (a) cost-bearing fire (runnable-but-not-free), (b) new spec / larger structure (needs design first), (c) intractable-limit note (work past the math/physical ceiling — record AS the limit, e.g. exact large-N MIP is super-exponential), (d) switch/close. Emit `⏸️ $0-runnable lane exhausted — loop PAUSES (not 100%-done; frontier toward the physical/math limit stays open)`. Reserve a literal `✅ done/terminal` ONLY for a genuinely FINITE-scope domain with ALL listed criteria met AND no open physical-limit frontier (rare). When in doubt, PAUSE — never declare 100%."
  dont = "emit `✅ domain depleted — loop terminates` / `100% closure` for an exploratory or perf or limit-bounded domain (contradicts feedback-closure-is-physical-limit; e.g. IIT4-Φ LIFE domain declared 'depleted/terminal' when large-N faithful-Φ + full-IIT4-CES frontier was still OPEN — only the $0 lane was empty) · treat 'AXES depleted / backlog absorbed' as domain-done when an above-$0 frontier remains · conflate 'no $0 runnable task right now' with 'reached the limit'"

@D ssot_freshness := "verify the working-tree <NAME>.md is the LIVE SSOT (not a stale/untracked shadow) before trusting its milestone set or declaring depletion" :: skill [required active]
  do   = "Stage 1 (before enumerating) AND Stage 5 (before declaring depletion): run a fail-open freshness pre-check on the active `<NAME>.md` — (a) UNTRACKED: `git ls-files --error-unmatch <NAME>.md` nonzero ⇒ the working-tree copy is a local-only shadow; (b) BEHIND-main: `git log --oneline HEAD..origin/main -- <NAME>.md` non-empty ⇒ origin/main has newer milestones/@goal your tree lacks. If EITHER fires, do NOT trust '0 open milestones' / depletion from the local copy — SURFACE `⚠ stale-SSOT: <NAME>.md untracked|behind-main — reconcile before depletion (git diff HEAD origin/main -- <NAME>.md · git checkout origin/main -- <NAME>.md)` and reconcile (or ask the user) before proceeding. `/domain set` already prints this same warning (domain `_stale_shadow_warn`); /cycle re-checks because depletion is the destructive read. Evidence: anima LIFE on an orphan-recover branch untracked root LIFE.md → working tree was the stale '$0-frontier-closed' version (all `[x]`, no perpetual @goal) while origin/main held the live 'perpetual engine' version with open axes A-D → /cycle wrongly declared depletion."
  do   = "**FILE-fresh ≠ CONTENT-fresh** — this git probe only checks the FILE vs origin/main; a `<NAME>.md` that MATCHES origin/main (probe PASSES) can still be CONTENT-stale: its milestone checkboxes / a section explicitly anchored at an old commit lag the actual code reality (nobody flipped the boxes after the work landed). That staleness is caught NOT here but by `@D dup_race_precheck` scan-B (per-item merged-PR / git-log evidence). So when the open-count is LARGE (≫ the round cap) OR the doc carries an old-commit anchor / a `doc-lag · flip to [x]` / `GENUINELY OPEN @ <old-sha>` style section, treat the file-fresh PASS as INSUFFICIENT and run scan-B on EVERY open item before trusting the next-list — evidence-flip the already-landed ones (a reconciliation PR) FIRST, then enumerate the genuine remainder. Evidence: RUNTIME.md was file-fresh (== origin/main, this probe PASSED) yet its Phase-1 Tier-A list was anchored at cycle-48 `[ ]` while the binary was at 0 externs (north-star MET, #1058/#1059) — 88 boxes were stale-done; scan-B (e.g. `_gmtime_r` #1053) caught it, #1186 reconciled (open 129→41)."
  dont = "enumerate or declare depletion off an untracked / behind-main `<NAME>.md` without surfacing the staleness · auto-overwrite or `git checkout` the local copy yourself (the user may hold intentional local edits — surface + let them reconcile) · run the git probe outside a git repo or with no origin/main ref (fail-open: skip silently when `git rev-parse` / `rev-parse origin/main` fails) · treat a file-fresh PASS as CONTENT-fresh (file == origin/main does NOT prove the checkboxes match reality — large open-count or an old-commit-anchor section demands scan-B on every open item first; skipping it re-runs already-landed work, the RUNTIME stale-6-extern miss)"

@D perpetual_domain := "a domain whose @goal declares no-termination NEVER emits a terminal/100% closure — its depletion is ALWAYS a lane-pause + re-seed from declared perpetual axes" :: skill [required active]
  do   = "Stage 1: detect a PERPETUAL marker in the active `<NAME>.md` @goal/body (`종료 조건 없음` · `완료되지 않` · `100% 미도달` · `미도달 = 설계` · `영구` · `끝없` · `perpetual` · `open horizon` · `no termination` · `never terminal` — same set as domain `_is_perpetual`; `/domain set` badges it `♾️ perpetual`). For a perpetual domain the Stage-5 depletion branch MUST (1) FIRST attempt to re-seed from the domain's own declared perpetual-axis backlog — an `## 영구 축` / `## perpetual axes` (or `## deferred`) section with open `- [ ]` — promoting the next batch as milestones before reporting any stop; (2) only if that backlog is ALSO drained, emit the `♾️ perpetual domain — current-tier lane drained; NO terminal closure by declaration (per feedback-closure-is-physical-limit); re-seed from declared axes (/domain milestone) or /schedule the next tier` form. Strengthens @D depletion_not_terminal (which says 'when in doubt PAUSE') by HARD-FORBIDDING the `✅ … terminates` branch whenever the marker is present."
  dont = "emit `✅ domain depleted — loop terminates` / `100% closure` for a domain whose @goal declares no-termination (the marker makes terminal closure a contradiction) · skip the declared `## 영구 축` / `perpetual axes` backlog when seeding (that backlog is the PRIMARY perpetual seed signal) · treat a perpetual domain's drained $0 lane as domain-done · re-classify a domain as finite just because the current-tier lane is empty"

@D oversized_split := "≥2 consecutive throttle deaths on ONE milestone ⇒ split into independently-landable sub-slices, never re-fire whole" :: skill [required active]
  do   = "track per-milestone throttle-death count across the loop; on the 2nd consecutive rate-limit death of the SAME milestone (agent dies with no PR after a long tool-use run), STOP re-firing it whole — split into the smallest independently-landable sub-slices (e.g. promote-only → wire → calibrate) and fan each as its own bounded Agent, flip the parent only when all slices land. Evidence: VERIFY-KIT V5 (IIT engine) died twice at 122/107 tool-uses fired whole; split into V5.1 promote (landed 308s) + V5.2 wire + V5.3 calibrate, each landing alone."
  dont = "re-fire an oversized milestone whole after ≥2 throttle deaths (same death loop) · split after a SINGLE transient death (retry whole first — split is the 2nd-death escalation) · split into slices that aren't independently landable (each must commit + PR alone) · flip the parent milestone before every slice lands"

@D handoff_debt_ledger := "cross-repo handoff INBOX edits left uncommitted in a target repo's dirty tree = tracked debt, cleared via isolated worktree, never force-committed in the dirty tree" :: skill [required active]
  do   = "when a fan-out files a cross-repo handoff (g60) into a target repo whose tree is dirty/non-main, the agent writes the working-copy INBOX edit but does NOT commit (shared-tree hazard) — record it as a HANDOFF DEBT line in the round report (`⚠ handoff debt: <repo> INBOX uncommitted`). Clear accumulated debts in a dedicated round: per target repo `git worktree add /tmp/wt-<repo>-inbox origin/main`, re-apply the entry, commit + push (PR if main gated), cleanup the worktree (g48 ack completion). Evidence: ARXIV A2/A3/A4 filed anima/demiurge/phanes INBOX as working-copy edits on dirty feature branches → A6 cleared all 3 via isolated worktrees off each repo's origin/main."
  dont = "force-commit a handoff INBOX edit in a target repo's dirty/feature-branch tree (sweeps unrelated staged work) · drop a handoff debt silently (always surface in the round report) · leave debts unbounded (clear them in a dedicated round once ≥2 accumulate) · touch the target repo's existing worktree/branch (isolated worktree off THEIR origin/main only)"

@D round_lint := "every loop round self-audits against the cycle-family contract before ending — THE LINTER" :: skill [required active]
  do   = "at each round's tail (before the ScheduleWakeup / depletion call) emit a one-line round-lint verdict auditing that the round honored: (1) resource-partition declared — disjoint concurrent · shared-exclusive serial (@D resource_contention) · (2) every spawned Agent carried the checkpoint-commit + throttle-resume clause (@D throttle_resilience) · (3) dup-race precheck A+B ran, SKIPs surfaced (@D dup_race_precheck) · (4) worktree leak-sweep ran (@D worktree_leak_cleanup) · (5) per-round Agent count ≤ cap (default 3 · g66) · (6) any cross-repo handoff recorded as debt (@D handoff_debt_ledger) · (7) any ≥2-throttle-death milestone was split, not re-fired whole (@D oversized_split) · (8) each landed item reported with an HONEST verify tier (🔵/🟢/🟡/🟠/🔴, no over-claim · g3/g5) + a progress bar (g56) · (9) SSOT freshness checked before any depletion call (@D ssot_freshness) · (10) a perpetual domain emitted NO terminal closure (@D perpetual_domain). Emit `🔍 round-lint: <N>/10 ✓` and flag any ✗ with its rule id. A failing check is surfaced + corrected, never silently passed."
  dont   = "end a round without the round-lint line · rubber-stamp a check that did not actually happen (honest self-audit, not a green-by-default) · accept a ✗ without correcting or surfacing it · let the lint block exceed one compact line-group"

@D micro_exp_handoff := "/cycle-bg ↔ /micro-exp build vs fire phase decision tree (mirrored in skills/micro-exp/SKILL.md · domain-agnostic)" :: skill [required active]
  do   = "when a candidate matrix is in context, walk the tree before dispatching: (NO matrix) → /kick (discovery first) · (matrix exists AND ALL candidates have dispatch infra <runnable>+<inputs>+<parser>+<workdir>) → /micro-exp <scope> (FIRE phase only) · (matrix exists AND any candidate is missing infra) → /cycle-bg <domain> (BUILD phase — each worktree agent writes its candidate's dispatch infra), then /micro-exp once infra is M/M ready. /cycle-bg is the canonical BUILD lane for candidate orchestration (build+fire integrated per worktree agent); /micro-exp is the FIRE-only lane (pre-built infra assumed). Domain-agnostic — kind is abstract (materials wall · LLM bench · web smoke · build bench are all examples among many)"
  dont = "fan-out /cycle-bg as fire phase when infra is already ready M/M (use /micro-exp directly — /cycle-bg's per-worktree overhead is wasted on a fire-only sweep) · skip /cycle-bg's build phase and fire /micro-exp on candidates with missing infra (Stage 1.5 in /micro-exp halts this with a steer; the silent-skip / hand-fabricate failure mode breaks honest sweep g63)"

## Decision tree — `/cycle-bg` ↔ `/micro-exp` (domain-agnostic)

```
candidate matrix in context?
├─ NO        → /kick                       (no candidates · discovery first)
└─ YES → dispatch infra exists for all candidates?
         ├─ NO       → /cycle-bg <domain>  (BUILD phase · each worktree agent writes its candidate's infra)
         ├─ PARTIAL  → /cycle-bg <domain>  (build the missing N) then /micro-exp <scope> (FIRE all)
         └─ YES      → /micro-exp <scope>  (FIRE phase only · pre-built infra assumed)
```

`/cycle-bg` = BUILD lane (build+fire integrated per worktree agent). `/micro-exp` = FIRE lane (Stage 1.5 auto-halts to the build phase on any missing prereq).

## Family — all four drain to depletion, differ only in entry shape

All four commands now drain the active domain's `## deferred` backlog to DEPLETION (open milestones = 0 AND deferred empty AND no other signal). They share the per-round cap (WIDTH throttle), the full 5-stage structure, the plan table, the dup-race precheck, and the leak guardrails. They differ ONLY in how they enter and pace the loop:

- **`/cycle`** — the plain entry. Self-generates the next batch from open milestones (else auto-seeds from `deferred`), then **auto-continues via ScheduleWakeup** each round until depletion. No phase-0 brainstorm; no external `loop` skill.
- **`/cycle-full`** — `/cycle` preceded by a **one-time phase-0 depletion brainstorm** (width-first idea exhaustion, cap 8 rounds). After the first fan-out it auto-continues exactly like bare `/cycle` (plain `/cycle` semantics, no re-brainstorm), draining to the same depletion condition.
- **`/cycle-loop`** — bare `/cycle` handed to the built-in `loop` skill for **explicit continuous intent + dynamic ScheduleWakeup pacing**. Same depletion termination; use when you want the loop's pacing/interval surface rather than bare `/cycle`'s inline self-continue.
- **`/cycle-full-loop`** — `/cycle-full` once (one-time brainstorm + first fan-out), then handed to the `loop` skill with plain `/cycle` as the recurring payload. Same depletion termination.
- **`/cycle-all`** — "run everything": same 5 stages but with NO per-round cap (enumerate the FULL open-milestone set + promote the ENTIRE `## deferred` backlog in one round; `cap>8→confirm` waived) and NO recommend/select gate (no "recommended priority" shortlist, no "which to dispatch?" hand-back — fan out EVERY PROCEED row immediately). Keeps ALL safety guardrails — SSOT-freshness, dup-race SKIP, leak-sweep, and CRITICALLY resource-contention serialization (fanning out everything still serializes shared EXCLUSIVE GPU resources — never parallel-fires every Agent). For 전부-진행 intent over a curated batch.

Bare `/cycle` and `/cycle-loop` now reach the same depletion end-state; the (now-thin) distinction is the entry/pacing surface (bare = inline self-continue · `-loop` = explicit `loop`-skill pacing). Both are kept — removing a published command is out of scope.
