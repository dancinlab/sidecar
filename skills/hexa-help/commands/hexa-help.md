---
description: /hexa-help [verb] — runs `hexa --help` (no arg) or `hexa <verb> --help` (with arg). Use to check signatures before guessing. Per commons g7.
argument-hint: "[verb]"
allowed-tools: Bash
---

!`sh "$CLAUDE_PLUGIN_ROOT/bin/hexa-help.sh" $ARGUMENTS`
