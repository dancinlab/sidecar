---
description: /kick <natural-language seed> — runs `hexa kick --seed "<seed>"` (gap breakthrough · discovery engine). All args join into the seed. For advanced flags (--rounds N, --engine mk9|mk10) call `hexa kick` directly. Aliased to `hexa drill`.
argument-hint: "<seed expression — natural language allowed>"
allowed-tools: Bash
---

!`hexa kick --seed "$ARGUMENTS"`
