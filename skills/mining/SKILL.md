---
name: mining
description: /mining — lens-driven divergence + pruning workflow. Sibling 3rd pillar to /domain (snapshot · log · MINING). Bundled lenses (same-formula · ouroboros · dimensional · tension · combinatorial · custom); verbs bare/lens/append/cycle-new/depletion/tree.
---

@D mining := "lens-driven divergence + connect-driven convergence · (leaves, edges) graph · 3rd pillar of /domain" :: skill [active]
  do   = "active-domain only (read /domain active pointer) · file pair <NAME>.mining.md (## cycles · ## leaves · ## edges) + <NAME>.mining.tape · two cycle kinds — lens (divergence: add leaves) + connect (convergence: add edges) · depletion on 0 new leaves under current lens OR 0 new edges in a full pass · ouroboros self-reference → auto-close goal · lens catalog bundled (extensible ~/.sidecar/lens/<name>.md) · connect excludes trivial-transitive / re-packaging / generic-ancestor edges · saturate auto-loops connect until depletion"
  dont = "off-domain mining (commons @D g58) · overwrite past cycles (append-only) · skip the cycle header for a new lens or connect cycle · keep firing a depleted lens or connect · mix mining content into <NAME>.md or <NAME>.log.md · forget the .tape idea-cart for surfaced @X candidates · count trivial-transitive A↔C as a new edge when A↔B + B↔C already exist"
