---
name: mining
description: /mining — lens-driven DIVERGENCE + connect-driven CONVERGENCE + tidy-driven ORGANIZE workflow. Sibling 3rd pillar to /domain (snapshot · log · MINING). Bundled lenses (same-formula · ouroboros · dimensional · tension · combinatorial · custom); 12 verbs — bare/lens/append/cycle-new/depletion/tree (divergence) · connect/edges/graph/saturate (convergence) · tidy/consolidate/squash (organize).
---

@D mining := "lens-driven divergence + connect-driven convergence + tidy-driven organize · 3rd pillar of /domain" :: skill [active]
  do   = "active-domain only (read /domain active pointer) · file pair <NAME>.mining.md (## cycles · ## leaves · ## edges) + <NAME>.mining.tape · three workflows — lens (divergence: add leaves) + connect (convergence: add edges) + tidy (organize: phase-group reorganize · LOSSLESS via chronological index table) · depletion on 0 new leaves under current lens OR 0 new edges in a full pass · ouroboros self-reference → auto-close goal · lens catalog bundled (extensible ~/.sidecar/lens/<name>.md) · connect excludes trivial-transitive / re-packaging / generic-ancestor edges · saturate auto-loops connect until depletion · tidy phase groups = divergence/analysis/convergence/external · tidy depth = light (header+index only) or full (regroup body) · squash = cosmetic dup-header dedup only · auto-suggest tidy when cycles ≥10 AND lines ≥500"
  dont = "off-domain mining (commons @D g58) · overwrite past cycles (append-only) · skip the cycle header for a new lens or connect cycle · keep firing a depleted lens or connect · mix mining content into <NAME>.md or <NAME>.log.md · forget the .tape idea-cart for surfaced @X candidates · count trivial-transitive A↔C as a new edge when A↔B + B↔C already exist · drop or rewrite leaf/edge content during tidy (must be lossless — chronological index preserves order) · guess a phase when @kind: is missing or ambiguous (refuse + ask to declare) · regroup body at --depth=light (light = header+index only)"
