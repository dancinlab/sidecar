---
name: mining
description: /mining — lens-driven DIVERGENCE + connect-driven CONVERGENCE + tidy-driven ORGANIZE workflow. Sibling 3rd pillar to /domain (snapshot · log · MINING). 0.4.0 — auto-saturate by default (lens + connect 둘 다 cycle 시작 시 round 사이 사용자 확인 없이 depletion 까지 self-continue; cap = 5 inner rounds per invocation, re-run hint when not yet depleted). New verb `auto` = full pipeline (divergence saturate all undepleted lenses → convergence saturate → tidy → done). Bundled lenses (same-formula · ouroboros · dimensional · tension · combinatorial · custom); 13 verbs — bare/lens/append/cycle-new/depletion/tree/auto (divergence) · connect/edges/graph/saturate (convergence) · tidy/consolidate/squash (organize).
---

@D mining := "lens-driven divergence + connect-driven convergence + tidy-driven organize · 3rd pillar of /domain · auto-saturate default" :: skill [active]
  do   = "active-domain only (read /domain active pointer) · file pair <NAME>.mining.md (## cycles · ## leaves · ## edges) + <NAME>.mining.tape · three workflows — lens (divergence: add leaves) + connect (convergence: add edges) + tidy (organize: phase-group reorganize · LOSSLESS via chronological index table) · depletion on 0 new leaves under current lens OR 0 new edges in a full pass"
  do   = "0.4.0 self-saturate default — lens AND connect rounds auto-loop until depletion (no per-round user gate); cap 5 inner rounds per invocation; emit 🔄 re-run hint when not yet depleted"
  do   = "ouroboros self-reference → auto-close goal · lens catalog bundled (extensible ~/.sidecar/lens/<name>.md) · connect excludes trivial-transitive / re-packaging / generic-ancestor edges"
  do   = "`auto` verb = full pipeline — divergence saturate (all undepleted bundled lenses round-robin) → convergence saturate → tidy --depth=light → done; one invocation drains the whole mining graph to depletion"
  do   = "tidy phase groups = divergence/analysis/convergence/external · tidy depth = light (header+index only) or full (regroup body) · squash = cosmetic dup-header dedup only · auto-suggest tidy when cycles ≥10 AND lines ≥500"
  dont = "off-domain mining (commons @D g58) · overwrite past cycles (append-only) · skip the cycle header for a new lens or connect cycle · keep firing a depleted lens or connect"
  dont = "pause between rounds for user confirmation (0.4.0 — default self-saturate · the only halt is depletion or cap-5 reached)"
  dont = "mix mining content into <NAME>.md or <NAME>.log.md · forget the .tape idea-cart for surfaced @X candidates · count trivial-transitive A↔C as a new edge when A↔B + B↔C already exist · drop or rewrite leaf/edge content during tidy (must be lossless — chronological index preserves order) · guess a phase when @kind: is missing or ambiguous (refuse + ask to declare) · regroup body at --depth=light"
