---
name: paper
description: Arxiv-style LaTeX paper scaffolder (v0.10 — PAPER.tape roster). Verbs — list [--sync] · set <slug> · show [<slug>] · goal <text> · title <text> · ms <text> · done <match> · todo <text> · new <slug> · sample [<name>] [<dest>] · samples · monograph-init <slug> [N] · fill <letter> · fig <size> <prompt> <out> · compile [dir] · lint [dir] · companion {init|sync} · outline [dir] · pipeline ... · atoms <domain> · verify-block ... · bib add <id> · pr-roll <repo> <since> · arxiv-prep [<dir>] · help. v0.10 adds a DOMAINS.tape-style roster — PAPER.tape (slug → dir) at repo root + per-paper PAPER.md (snapshot: @title · @goal · `- [ ]` milestones) + PAPER.log.md (append-only step log); progress/title read LIVE from PAPER.md so the roster never churns on a milestone flip. new/sample/monograph-init auto-register + scaffold. bare /paper shows the active paper. Old `list` (bundled samples) is now `samples`. v0.9 monograph mode preserved (`monograph-init` scaffolds \appendix + N appendix stubs + companion/; `fill` per-appendix runbook; lint auto-tier paper/monograph; companion sync). FOUR bundled samples — sample-nb-bcs-absorbed · sample-fusion-7gate · sample-cost-routing · sample-blue-max. Triggers — "논문 만들어", "monograph", "모노그래프", "paper scaffold", "new arxiv paper", "arxiv 템플릿", "compile this paper", "fal.ai figure for paper", "paper 등록", "paper 진행도", "PAPER.tape", "/paper list", "/paper set", "/paper monograph-init", "/paper fill", "/paper sample fusion", "/paper sample cost-routing", "/paper sample blue-max".
allowed-tools: Bash
---

@D paper := "arxiv-style LaTeX paper scaffolder + PAPER.tape roster (slug → dir; progress/title derived from PAPER.md)" :: skill
  do   = "`/paper {list|set|show|goal|title|ms|done|todo|new|sample|samples|monograph-init|fill|fig|compile|lint|companion|outline|pipeline|atoms|verify-block|bib|pr-roll|arxiv-prep}` — register, track, scaffold, run, verify, fetch, ship"
  dont = "duplicate the image generator — `/paper fig` delegates to the sister `imagine` plugin · store progress/@title IN PAPER.tape (stays DERIVED from PAPER.md — no churn)"
