---
description: /paper <args> — arxiv-style LaTeX paper scaffolder (v0.10 — PAPER.tape roster). Verbs — list [--sync] · set <slug> · show [<slug>] · goal <text> · title <text> · ms <text> · done <match> · todo <text> · new <slug> · sample [<name>] [<dest>] · samples · monograph-init <slug> [N] · fill <letter> · fig <size> <prompt> <out.png> · compile [dir] · lint [dir] · companion {init|sync} · outline [dir] · pipeline ... · atoms <domain> · verify-block ... · bib add <id> · pr-roll <repo> <since> · arxiv-prep [<dir>] · help. v0.10 adds a DOMAINS.tape-style roster — PAPER.tape (slug → dir) at repo root + per-paper PAPER.md (snapshot: @title · @goal · `- [ ]` milestones) + PAPER.log.md (append-only step log); progress/title read LIVE from PAPER.md so the roster never churns on a milestone flip. new/sample/monograph-init auto-register + scaffold. bare /paper shows the active paper. Old `list` (bundled samples) is now `samples`. v0.9 monograph mode preserved. FOUR bundled samples — sample-nb-bcs-absorbed · sample-fusion-7gate · sample-cost-routing · sample-blue-max.
argument-hint: "list [--sync] | set <slug> | show [<slug>] | goal <text> | title <text> | ms <text> | done <match> | todo <text> | new <slug> | sample [<name>] [<dest>] | samples | monograph-init <slug> [N] | fill <letter> | fig <size> <prompt> <out.png> | compile [dir] | lint [dir] | companion {init|sync} | outline [dir] | pipeline ... | atoms <domain> | verify-block ... | bib add <id> | pr-roll <repo> <since> | arxiv-prep [<dir>] | help"
allowed-tools: Bash
---

!`R="$CLAUDE_PLUGIN_ROOT"; H="$R/bin/_paper.hexa"; [ -f "$H" ] || { V="$(ls -1 "$HOME/.claude/plugins/cache/sidecar/paper" 2>/dev/null | sort -V | tail -1)"; [ -n "$V" ] && { R="$HOME/.claude/plugins/cache/sidecar/paper/$V"; H="$R/bin/_paper.hexa"; }; }; hexa run "$H" --root "$R" $ARGUMENTS`
