# sample-fusion-7gate — D-T fusion 10-stage pipeline starter

Minimal stub (1-page main.tex) showing the §Full Pipeline pattern fully populated with the 10-stage D-T fusion fuel journey. NOT a full copy of hexa-fusion-7gate — just enough to show how stage table cells get filled (atom id × tier × backing reference).

Copy via:

```
/paper sample sample-fusion-7gate <your-slug>
```

Then edit `main.tex` to relabel stages for your domain. Stages 1–3 are the fuel cycle, 4–8 are ignition physics, 9–10 are recovery + handoff.
