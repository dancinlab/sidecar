---
description: /pool <args> — runs `pool "$@"` (host roster + remote exec). Verbs — list · add <host> · on <host> <cmd> · status · install tailscale · rm <host>. Pass args through unchanged.
argument-hint: "<list | add <host> | on <host> <cmd> | status | install tailscale | rm <host>>"
allowed-tools: Bash
---

!`pool $ARGUMENTS`
