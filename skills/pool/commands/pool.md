---
description: /pool <args> — runs `pool "$@"` (host roster + remote exec). Verbs — list · add <host> · on <host> <cmd> · status · install tailscale · rm <host> · off <host>. Pass args through unchanged.
argument-hint: "<list | add <host> | on <host> <cmd> | status | install tailscale | rm <host>>"
allowed-tools: Bash
---

!`sh "$CLAUDE_PLUGIN_ROOT/bin/pool.sh" $ARGUMENTS`
