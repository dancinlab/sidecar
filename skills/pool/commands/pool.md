---
description: /pool <args> — runs `sidecar pool "$@"` (sidecar-owned host roster + remote exec; canonical entry). Verbs — list · add <host> · on <host> <cmd> · status · health · route · bg · refresh · init · rm <host> · clean · desc. Pass args through unchanged.
argument-hint: "<list | add <host> <user@host> | on <host> <cmd> | status | route <cmd> | clean | rm <host>>"
allowed-tools: Bash
---

!`sidecar pool $ARGUMENTS`
