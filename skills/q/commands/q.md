---
description: /q — quick side-question alias for the built-in /btw. Pass a short question or remark and the model answers briefly (1-3 sentences) without pivoting from the current task — same semantics as /btw, shorter to type.
argument-hint: "<your question>"
---

Quick side-question (via `/q`, alias for built-in `/btw`):

`$ARGUMENTS`

Answer briefly — 1-3 sentences. Do NOT pivot from the current task or start a new long thread. After the short answer, resume the main work exactly where it was.
