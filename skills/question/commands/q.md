---
description: /q — 2-character alias for `/question` (which is itself an alias for built-in /btw). Same semantics — answer briefly (1-3 sentences) without pivoting current task.
argument-hint: "<your question>"
---

Quick side-question (via `/q`, alias for `/question` / built-in `/btw`):

`$ARGUMENTS`

Answer briefly — 1-3 sentences. Do NOT pivot from the current task or start a new long thread. After the short answer, resume the main work exactly where it was.
