---
name: system
description: /system — campaign control-tower (domain-agnostic). One persistent "mission control" over EVERY in-flight job of a long-running campaign across ANY execution surface — rented pods (vast/runpod via cloud) · pool hosts · local procs. Ties the manifest (cwd ./pods.json) + event-driven watchers + harvest+verdict + the autonomous harvest→re-dispatch loop into one command. NOT domain-locked — works for DFT sweeps · ML training fleets · build matrices · render farms · any N-parallel-job campaign with a queued backlog. Verbs — bare/status (unified dashboard: job · surface · stage · progress · cost · verdict + budget bar + queue depth) · watch (arm one event-driven watcher per active job; terminal = DONE-marker / error-trace / process-gone-2x debounced; background, no ScheduleWakeup, rate-limit-survivable) · harvest [<id>] (terminal job → pull artifacts + parse domain metric + verdict tier 🔵/🟢/🟠/🔴 per g5 · persist ledger) · next/redispatch (autonomous loop: harvested-terminal → atlas register on 🟢 (g62) → fire next queued candidate → arm its watcher · NO user gate) · auto (full loop status→watch→harvest→next until backlog drained OR budget hit OR interrupt) · cost (budget tracker · per-surface) · queue [add|rm|ls] (candidate backlog). Triggers — "/system", "관제탑", "캠페인 현황", "전체 잡 현황", "mission control", "campaign status", "harvest 후 자동발사", "결과보고 추가발사", "잡 전부 모니터", "자율 재발사 루프", "control tower".
allowed-tools: Bash, Read, Write, Edit
---

@D system := "campaign control-tower — unified dashboard + event-driven watch + harvest→verdict + autonomous re-dispatch loop · domain-agnostic" :: skill [active]
  do   = "single mission-control over ALL in-flight jobs across surfaces (pod/pool/local) — read cwd `./pods.json` manifest SSOT (cloud dispatch) + a `queue` backlog + a `budget` cap; never reinvent the manifest"
  do   = "abstract job model — job = {id, surface, workdir, terminal_marker, metric_parser, verdict}; NO domain assumptions (DFT `CHAIN DONE` · ML `training complete` · build `PASS` are all just terminal_marker strings)"
  do   = "`watch` arms event-driven watchers (background while-loop polling a durable log · debounced terminal · no ScheduleWakeup · rate-limit-survivable per g10); `harvest` parses the per-job metric + emits a g5 verdict tier VERBATIM (no LLM self-judge)"
  do   = "EXIT-CODE-AWARE terminal taxonomy — DONE (marker ∧ no trailing STOP/error/Maximum-CPU) · TIMEOUT-RESUMABLE (max_seconds → recover, NOT success even if the marker printed) · CRASHED (STOP n≠0 / Error in routine) · transport 255 = TRANSIENT-GATEWAY (TCP-open+contract-live, retry) vs POD-DOWN (TCP-closed). NEVER trust a bare terminal-marker grep (a routine prints `JOB DONE` even on a non-zero walltime stop)"
  do   = "upstream-reflex (g59) — when watch/harvest hits a `hexa cloud` CLI gap (false-terminal · transport ambiguity · missing preflight axis), file `~/core/hexa-lang/INBOX.log.md` SAME-TURN with verbatim evidence; the caller-side trailing-scan is a STOPGAP, the durable fix is the CLI's 3-tier exit code — leave an upstream trail, don't bake a silent permanent workaround"
  do   = "`upstream [<repo>]` verb = report that trail — grep linked-repo INBOX.log.md (campaign tag) + `gh pr list --state merged --search` → table of what this campaign filed upstream (PR# · slug · status). makes 'hexa upstream fix in this session' a one-verb query; flag any gap hit-this-turn-but-unfiled"
  do   = "`next`/`auto` = the autonomous harvest→re-dispatch loop — on a terminal verdict, atlas register on 🟢 (g62) → fire the next queued candidate → arm its watcher · 🔴 FALSIFIED is a valid terminal (g63) → still auto-advance · no 'shall I continue?' gate (same autonomy as the initial cost-bearing fire)"
  do   = "queue status = autonomy contract — `queued` auto-fires · `blocked:<technical>` (missing coord/data/pseudo/dep) AUTO-RESOLVES (arxiv/fetch/build) then fires · only `gated:<human-only>` (credential · irreversible · design decision) stops for a human · NO 'awaiting-approval' status for an ordinary candidate"
  do   = "honor budget — `auto` halts on declared cap breach (g64); state spend in one line per re-dispatch; provider/cost from pods.json"
  dont = "domain-lock to one campaign kind · duplicate `/cloud pods` (reuse its `./pods.json`) or `/micro-exp` (that LAUNCHES a fresh sweep; /system is the persistent CONTROL TOWER over already-running jobs)"
  dont = "pause for user approval after each verdict · halt the loop on a single result · treat a depleted wave as terminal while the queue holds candidates or open axes remain · model-side tail -f / sleep-poll (use the detached watcher)"
  dont = "LLM-judge correctness (g5 — paste the verify verdict verbatim) · fabricate a fire/harvest that did not happen (g63 honesty) · arm a watcher on a job whose surface is unreachable (report the transport failure instead)"
