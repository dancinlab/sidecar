---
description: /verify <args> — runs `hexa verify "$@"`. Forms — `hexa verify <id>` (atlas atom lookup + recompute), `hexa verify --expr <fn> <n> <v>` (numerical), `hexa verify --fence "<claim>"` (honest ⚪ speculation-fenced), `hexa verify rubric` (tier rubric). NL claims that don't match a form trigger usage — model can reframe and retry.
argument-hint: "<id | --expr <fn> <n> <v> | --fence \"<claim>\" | rubric>"
allowed-tools: Bash
---

!`hexa verify $ARGUMENTS`
